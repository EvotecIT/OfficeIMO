name: Actions Storage Cleanup

on:
  schedule:
    - cron: '17 */6 * * *'
  workflow_dispatch:
    inputs:
      apply:
        description: 'Apply deletions (true/false)'
        required: false
        default: 'true'

permissions:
  actions: write
  contents: read

concurrency:
  group: actions-storage-cleanup
  cancel-in-progress: false

jobs:
  cleanup:
    uses: EvotecIT/PSPublishModule/.github/workflows/reusable-github-housekeeping.yml@bbf70fcadad5a2fc28fd3749685d35baf81d3e4e
    with:
      config-path: ./.powerforge/github-housekeeping.json
      apply: ${{ github.event_name == 'workflow_dispatch' && inputs.apply == 'true' || github.event_name != 'workflow_dispatch' }}
      powerforge-ref: bbf70fcadad5a2fc28fd3749685d35baf81d3e4e
    secrets:
      github-token: ${{ secrets.GITHUB_TOKEN }}
