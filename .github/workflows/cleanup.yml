name: Actions Storage Cleanup

on:
  schedule:
    - cron: '17 */6 * * *'
  workflow_dispatch:
    inputs:
      apply:
        description: 'Apply deletions (true/false)'
        required: false
        default: 'true'

permissions:
  actions: write
  contents: read

concurrency:
  group: actions-storage-cleanup
  cancel-in-progress: false

jobs:
  cleanup:
    uses: EvotecIT/PSPublishModule/.github/workflows/reusable-github-housekeeping.yml@2831df4ed857da88f87537a77887b7bc44ffc7c8
    with:
      config-path: ./.powerforge/github-housekeeping.json
      apply: ${{ github.event_name == 'workflow_dispatch' && inputs.apply == 'true' || github.event_name != 'workflow_dispatch' }}
      powerforge-ref: 2831df4ed857da88f87537a77887b7bc44ffc7c8
    secrets:
      github-token: ${{ secrets.GITHUB_TOKEN }}
