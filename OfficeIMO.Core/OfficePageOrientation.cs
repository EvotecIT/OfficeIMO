namespace OfficeIMO;

/// <summary>
/// Specifies whether a document page is taller or wider than its normal reading direction.
/// </summary>
public enum OfficePageOrientation {
    /// <summary>The page is taller than it is wide.</summary>
    Portrait,

    /// <summary>The page is wider than it is tall.</summary>
    Landscape
}
