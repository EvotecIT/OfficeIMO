namespace OfficeIMO.PowerPoint {
    /// <summary>
    ///     Specifies how a stack should be positioned along its primary axis.
    /// </summary>
    public enum PowerPointShapeStackJustify {
        /// <summary>Start at the leading edge.</summary>
        Start,
        /// <summary>Center within the bounds.</summary>
        Center,
        /// <summary>End at the trailing edge.</summary>
        End
    }
}
