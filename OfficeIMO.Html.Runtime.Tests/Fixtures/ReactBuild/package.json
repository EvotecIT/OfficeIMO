{
  "name": "officeimo-runtime-react-build-fixture",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild src/main.jsx --bundle --format=esm --splitting --minify --target=es2020 --outdir=dist --entry-names=app --chunk-names=chunk-[name]-[hash]"
  },
  "dependencies": {
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "esbuild": "0.25.12"
  }
}
