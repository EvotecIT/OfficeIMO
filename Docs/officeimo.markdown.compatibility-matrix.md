# OfficeIMO.Markdown Compatibility Matrix

This matrix tracks the work needed to make `OfficeIMO.Markdown` a credible Markdig-class Markdown engine while keeping the OfficeIMO-specific document strengths explicit.

Status values:

- `Covered`: behavior is implemented and protected by focused tests.
- `Partial`: behavior exists, but coverage or edge-case conformance is incomplete.
- `Intentional`: OfficeIMO behavior deliberately differs from CommonMark, GFM, or Markdig.
- `Gap`: behavior is missing or too weak to claim.
- `Planned`: design direction is known, but implementation has not landed.

## Baseline

| Area | Current baseline |
| --- | --- |
| External comparison package | Markdig `1.3.2` in `OfficeIMO.Tests` and `OfficeIMO.Markdown.Benchmarks`; guarded by `PackageDependencyGuardrailTests.MarkdownParityProjects_UseTheSameCurrentMarkdigBaseline` and `MarkdownCompatibilityDocs_TrackCurrentMarkdigBaselineVersion`; verified current against NuGet and `dotnet list ... package --outdated` on 2026-06-28 |
| CommonMark reference | 316 of 652 official CommonMark `0.31.2` examples pinned as smoke fixtures |
| CommonMark full inventory | 652 of 652 official CommonMark `0.31.2` examples currently match; 0 failures are reported in `Docs/officeimo.markdown.commonmark-inventory.md` |
| GFM reference | 43 cmark-gfm extension smoke fixtures plus a focused upstream ignored-autolink crash regression |
| GFM inventory | 43 tracked GFM fixtures currently measured in `Docs/officeimo.markdown.gfm-inventory.md`: 40 upstream cmark-gfm fixtures, 3 OfficeIMO supplements, 43 passing, 0 failing |
| Markdig extension inventory | 33 Markdig extension-family rows in `Docs/officeimo.markdown.markdig-extension-inventory.md`: 6 covered, 11 partial, 4 intentional, 12 gap |
| OfficeIMO core package | `OfficeIMO.Markdown` owns parsing, semantic AST, syntax tree, writing, and HTML projection |
| Host renderer package | `OfficeIMO.MarkdownRenderer` owns WebView/browser shell rendering and incremental updates |
| Benchmark evidence | `OfficeIMO.Markdown.Benchmarks` compares parse/syntax-tree parse/HTML render against the current Markdig baseline and now includes transform/diagnostics cost lanes over stable README, transcript, technical-doc, rich-AST, long-list, large-table, and normalization-heavy corpora |

Detailed open work is tracked in `Docs/officeimo.markdown.markdig-parity-gap-plan.md`.

## Standards Coverage

| Capability | CommonMark profile | GFM profile | OfficeIMO profile | Evidence | Status | Next action |
| --- | --- | --- | --- | --- | --- | --- |
| ATX headings | Yes | Yes | Yes | Full official CommonMark `0.31.2` ATX heading example set; `Markdown_CommonMark_Examples_Tests`, `Markdown_Reader_Profile_Tests` | Covered | Keep source-span and lossless-trivia coverage aligned with the full spec cases |
| Setext headings | Yes | Yes | Yes | Full official CommonMark `0.31.2` Setext heading example set; Markdig parity cases; profile tests; list-item Setext syntax/source mapping tests; exact Setext underline marker syntax/source-field/source-edit tests | Covered | Keep source-span and lossless-trivia coverage aligned with the full spec cases |
| Paragraphs, entities, and breaks | Yes | Yes | Yes | 316 CommonMark smoke fixtures, including official paragraph blank-line/indentation, ATX heading, Setext heading, thematic-break, hard-break, soft-break, list-interruption, legal/illegal ordered-marker, nested-list, tab-expanded nested lists, backslash-escape, raw-HTML boundary, relaxed HTML block start, strict raw-HTML tag boundary fallback, blockquote-contained raw-HTML boundary rendering, invalid angle-autolink fallback, source-preserved raw inline HTML after invalid angle-link destinations, raw inline HTML line endings with hard-break-looking markers, final-line literal backslash handling, source-preserved line endings inside multiline code spans, non-breaking-space code-span comparison, link destination/title grammar, nested link-label precedence, linked-image reference-label grammar, link-label inline-span precedence for code spans/raw inline HTML/angle autolinks, unresolved reference-like backslash escape fallback, multiline reference-definition title grammar, multiline reference-label grammar, reference-definition paragraph-interruption grammar, blockquote-contained reference-definition grammar, named/numeric entity examples including invalid numeric replacement, container indentation/continuation boundaries, and emphasis delimiter edge cases for root dual italic runs, empty opposite-marker spans, same-marker nested strong/italic spans, and mixed-marker literal handling; renderer tests; hard-break/entity fixtures; exact ATX heading opening/closing-marker syntax/source-field/source-edit tests; hard-break marker syntax tokens plus native metadata/source-edit coverage for two-space, backslash, and inline HTML break spellings; decoded entity source-text metadata/source-edit coverage; fence info-string entity fixtures; full CommonMark inventory is green | Partial | Keep broader source-span and writer coverage aligned while AST/lossless work continues |
| Thematic breaks | Yes | Yes | Yes | Full official CommonMark `0.31.2` thematic-break example set, including positive marker-family, spaced-marker, invalid-marker, paragraph-boundary, list-boundary, nested-list-item, indentation, and indented-code boundary examples; reader tests; exact thematic-break marker syntax/source-field/source-edit tests; native thematic-break projection tests | Covered | Keep source-span/native projection coverage aligned |
| Fenced code blocks | Yes | Yes | Yes, plus semantic fenced blocks | Markdig parity cases, semantic fenced block tests, native opening/info/content/closing source-span and source-edit tests including blockquote/list container remapping, 29 CommonMark fence delimiter/indentation/container/info-string smoke fixtures | Partial | Add remaining full-spec inventory |
| Indented code blocks | Yes | Yes | Yes | Markdig parity cases, profile tests, official CommonMark root/list code-boundary fixtures including blank-line continuation and blockquote precedence; full official CommonMark inventory is green | Partial | Keep source-span and lossless-trivia coverage aligned with the full spec cases |
| Blockquotes | Yes | Yes | Yes, plus callout recognition by default | Markdig parity cases, compatibility notes, official CommonMark compact nested blockquote, lazy-continuation, adjacent-blockquote, container-boundary, indented-code precedence, inner-indentation threshold fixtures, nested list-item quote-to-ordered-list boundary regression coverage, empty blockquote rendering after reference-definition-only quotes, and exact quote marker syntax/source-field/source-edit tests; full official CommonMark inventory is green | Partial | Keep source-span/lossless coverage aligned and document intentional callout behavior |
| Ordered/unordered lists | Yes | Yes | Yes | Markdig parity cases, profile tests, invariant tests, official CommonMark shallow-indentation, tab-expanded nesting, HTML-comment boundary, nested blockquote list-continuation, nested indented-code continuation fixtures, syntax-only reference definitions inside list items preserved in final syntax trees, non-one ordered-marker child-list regression coverage after nested quotes, and exact list/task marker token syntax nodes plus native/editor source fields; full official CommonMark inventory is green | Partial | Continue canonical `ListItem` cleanup and broader Markdig/GFM list-extra decisions |
| Inline emphasis and escapes | Yes | Yes | Yes | Markdig parity cases plus CommonMark emphasis/backslash-escape smoke fixtures, including underscore opener/closer rejection, intraword underscore rejection across ASCII, digits, and CJK text, punctuation-adjacent underscore emphasis with syntax span coverage, same-marker nested strong/italic CommonMark edge cases, and source-backed formatting delimiter syntax token nodes for parsed strong/emphasis/strong-emphasis/strikethrough/highlight wrappers; the CommonMark `Emphasis and strong emphasis` section is currently green in the full inventory | Partial | Keep broader Markdig/GFM emphasis-extra and source/writer coverage honest |
| Links and reference links | Yes | Yes | Yes | Markdig parity cases, profile tests, parse-result/native reference-definition metadata and source-edit tests, reference-definition label-frame syntax token/native metadata/navigation tests, blockquote-contained reference-definition pre-scan/source-span coverage, inline and reference-style link target/title plus opening/separator/closing delimiter marker source-edit tests, CommonMark fixtures for decoded character references in link destinations/titles before URL attribute encoding, nested link-label precedence, unresolved shortcut-reference fallback, unresolved reference-like backslash escape fallback, linked-image reference labels, and label scanning that treats code spans, raw inline HTML, supported inline HTML wrappers, and angle autolinks as inline spans before accepting a closing bracket; the CommonMark `Links` section is currently green in the full inventory | Partial | Keep broader GFM/Markdig link-extension inventory and image/reference source metadata coverage aligned |
| Images | Paragraph inline images in standards profiles | Paragraph inline images in standards profiles | Standalone image block promotion enabled by default | Profile tests, syntax tests, inline and reference-style image alt/source/title plus opening/separator/closing delimiter marker source-edit tests, standalone and linked image block alt/source/title/link source-field/source-edit tests | Intentional | Keep standards behavior separate from OfficeIMO default promotion |
| Raw HTML | Optional | Optional with cmark-gfm tag filter render option | Optional | HTML block/inline tests, expanded CommonMark HTML block fixtures for raw tables, type 1 blocks, relaxed multiline type 1/type 6 starts, strict type 7 tag-name/attribute-boundary fallback, CommonMark-compatible multiline raw tag attributes, inline comments, processing instructions, declarations, CDATA, malformed raw-HTML fallback, `textarea` raw-text containers, comments, processing instructions, CDATA, declarations, and paragraph interruption, native raw/comment HTML projection and source-edit tests, source-backed HTML comment opening/body/closing token fields, source-backed simple raw HTML opening-tag/body/closing-tag fields for matched tag frames, source-backed processing-instruction, CDATA, and declaration opening-marker/body/closing-marker fields, renderer security-profile tests proving `Strip`, `Escape`, and `Sanitize` behavior across CommonMark HTML block types 1-7, a first-class `HtmlOptions.CreateGitHubFlavoredMarkdownProfile()` for cmark-gfm-style task-list, footnote, raw-HTML allow, and tag-filter rendering, and GFM HTML tag filter fixtures/tests for raw blocks and inlines; the CommonMark `Raw HTML` section is currently green in the full inventory | Partial | Keep broad CommonMark/GFM HTML corpus expansion moving and classify sanitizer allowlist differences |

## GFM And Extension Coverage

Generated scoreboards: `Docs/officeimo.markdown.gfm-inventory.md` tracks the current GFM fixture corpus by section and source so upstream cmark-gfm fixtures stay separate from OfficeIMO supplement cases. `Docs/officeimo.markdown.markdig-extension-inventory.md` tracks reflected Markdig extension-family entry points so extension parity is explicit instead of implied.

| Capability | Current behavior | Evidence | Status | Next action |
| --- | --- | --- | --- | --- |
| Pipe tables | Enabled in GFM and OfficeIMO profiles; GFM requires a delimiter row and keeps cells inline-only, while OfficeIMO can still opt into headerless/structured cells | Table parser tests, GFM smoke fixtures for escaped pipes, code-span pipes, escaped pipes inside table-cell code spans, broader table backslash escaping, no-leading-pipe tables, one-column delimiter rows, paragraph-to-table boundaries, alignment, header/delimiter mismatch rejection in both shorter and longer delimiter-row directions, body-row padding/truncation, reference links inside cells, adjacent empty cells, compact inline emphasis, inline formatting in headers/body cells, non-table pipe-row rejection, minimal header-only tables, raw inline HTML/break tags, and list/blockquote container ownership; alignment-row syntax/native span and source-edit tests; empty header/body cell syntax, semantic, native snapshot, and source-edit span tests; aligned Markdown writer roundtrip proof for escaped pipes, backslashes, padding, and reparsed GFM output | Covered | Keep the GFM table fixture corpus and table-cell source-edit coverage current |
| Task lists | Enabled in GFM and OfficeIMO profiles | GFM fixtures covering nested items, uppercase checked markers, whitespace after markers, and non-task bracket-marker list text; profile tests; ordered and unordered Markdown writer roundtrip tests; native marker source-field/snapshot/source-edit tests; GitHub HTML renderer tests | Covered | Keep GitHub HTML shape and marker-boundary tests current |
| Strikethrough | `~~text~~`; single-tilde in GFM profile | Profile tests, GFM nested-emphasis and delimiter-run fixtures | Covered | Expand full cmark-gfm delimiter edge corpus |
| Footnotes | Enabled in GFM and OfficeIMO profiles | GFM fixtures for GitHub HTML shape, first-reference ordering, repeated-reference backrefs, missing references, unused definitions, nested blockquote/code/paragraph bodies, and source spans for repeated reference labels; footnote definition/reference label, opening/separator markers, and body source fields; inline footnote reference delimiter marker metadata tests; structured footnote body Markdown writer roundtrip through the GFM parser and GitHub HTML renderer; GitHub backrefs render after final non-paragraph body blocks | Covered | Keep the GFM footnote fixture corpus and structured-body writer coverage current |
| Alerts/callouts | OfficeIMO callout extension; disabled in CommonMark/GFM profiles unless explicitly registered | Built-in extension tests and profile tests | Intentional | Keep registration explicit and matrixed |
| Autolinks | Configurable bare URL, selected GFM bare schemes, `www`, email, and angle autolinks; OfficeIMO/GFM profiles autolink plus-tag plain emails while CommonMark/portable profiles keep them literal; Markdig-style previous-character and domain-without-period options are exposed without changing OfficeIMO legacy defaults | Markdig parity cases, profile tests, 12 CommonMark autolink fixtures with all official CommonMark autolink examples passing in the full inventory, including unsafe-character percent encoding in `href` attributes and invalid angle-autolink fallback through paragraph text, GFM fixtures for bare URLs, bare `mailto:`/`xmpp:` URLs, Unicode destinations, plus-tag emails, invalid email-like tokens, quoted/trailing punctuation trimming, `www` host underscore rules, a focused upstream ignored-autolink crash regression, option coverage for domain-without-period and valid-previous-character behavior across top-level and table-cell parsing, and native/syntax metadata tests for source-backed bare/angle autolink targets plus angle marker source edits | Partial | Expand remaining extended-autolink coverage and finish source/writer evidence |
| Front matter | OfficeIMO default only | reader/profile tests plus syntax/native opening/closing fence and key/value source-field/source-edit coverage | Intentional | Keep front matter disabled in standards profiles |
| Semantic fenced blocks | OfficeIMO extension contract | semantic fenced block tests, native visual tests, renderer semantic-fence conversion tests | Intentional | Keep parser, AST, renderer, and native projection coverage aligned |
| Parser/render extensions | Block, fenced-block, inline parser, post-inline transform, type-targeted block/inline HTML and Markdown overrides, and syntax-kind-targeted block/inline HTML and Markdown overrides | block extension tests, block parser ordering/disabled-fallback tests, multi-placement parser conflict tests against reference definitions, GFM tables, and built-in callouts, custom block syntax-builder context coverage for owned child-container syntax and explicit inline-container associated objects, inline parser ordering/fallback/disabled-extension tests, syntax-aware custom inline native snapshot coverage, inline transform ordering/replacement/nested/span-preservation tests, custom renderer override tests including inline ordering/null-fallback behavior, contextual type-targeted block/inline HTML/Markdown renderers that can read body/write context, final syntax nodes, normalized source slices, original trivia-backed slices when available, and token/field source slices from `MarkdownSourceSpan`, syntax-kind block/inline HTML/Markdown renderers that run before type-targeted overrides and receive final syntax nodes plus source-slice helpers, nested block renderer dispatch coverage for built-in quote/list/callout/details/definition/footnote/table containers, public render-context child-block helpers for custom contextual containers, semantic fenced renderer tests proving code-block fallback is bypassed | Partial | Expose full lossless token/trivia extension points beyond source-slice-backed spans and keep custom container guidance aligned as external packages adopt the new helpers |

## AST And Editor-Grade Coverage

| Contract | Current behavior | Evidence | Status | Next action |
| --- | --- | --- | --- | --- |
| Semantic AST | `MarkdownDoc` plus typed block/inline model | public API, tree invariant tests, footnote block-primary cleanup including public structured child-block construction/projection and text-constructor child-block adaptation, list-item paragraph-block ownership, public canonical list-item child-block projection, generic syntax-child ownership, rewriter source-cache preservation tests, shared canonical child-syntax reconciliation for list items/blockquotes/callouts/definition-list terms and values/details/footnotes/table cells, cloned definition-list group syntax, stale syntax-child pruning after projection changes, first-class `DefinitionListTerm` ownership with the legacy inline `Terms` view preserved as a derived compatibility projection, callout structured body construction tests including text-constructor child-block adaptation, definition-list public child-block projection plus entry/group term and syntax-child ownership tests, table/table-cell child-block projection plus table and table-cell syntax-child ownership tests | Partial | Continue canonicalizing duplicated node shapes one type at a time |
| Syntax tree | `MarkdownSyntaxNode` with kind, literal, source span, children, custom kind, associated object | syntax tests, invariant tests, token-node coverage for heading levels, heading opening/closing/Setext underline markers, front matter fences, quote markers, list markers, task markers, thematic-break markers, reference-definition label-frame marker tokens, footnote-definition label-frame marker tokens, HTML comment opening/body/closing tokens, simple raw HTML opening-tag/body/closing-tag tokens for matched tag frames, processing-instruction, CDATA, and declaration opening-marker/body/closing-marker tokens, parsed inline formatting opening/closing marker tokens, code-span opening/content/closing tokens, backslash escape marker/character tokens, decoded entity source-text tokens, hard-break marker tokens, inline footnote reference delimiter marker tokens, and inline/reference link/image delimiter marker tokens | Covered | Keep expanding associated-object and source-token coverage |
| Original/final tree split | `MarkdownParseResult.SyntaxTree` and `FinalSyntaxTree` | transform and invariant tests | Covered | Add more transform replacement fixtures |
| Source spans | Line/column/offset-aware span model, with token-level spans for selected structured nodes | syntax tests, native document tests, heading level/text source-span tests including multiline Setext text payloads and exact Setext underline markers, exact ATX heading opening/closing-marker syntax/source-field/source-edit tests, paragraph payload source-field/source-edit tests, front matter key/value source-span tests, table alignment-row source-span tests, code-fence opening/info/content/closing source-span tests with blockquote/list/footnote remapping, nested quote-to-non-one ordered-list source-span remapping tests, callout kind/title/body source-span tests, details summary/body source-span tests, exact quote marker syntax/source-field/source-edit tests, exact list/task marker token syntax/source-field/source-edit tests, exact thematic-break marker syntax/source-field/source-edit tests, quote marker/body source-span tests, footnote definition/reference label and body source-span tests, footnote-definition label-frame syntax-token and native metadata tests, reference-definition label-frame syntax-token and native metadata tests, inline formatting delimiter syntax token tests with full-wrapper syntax spans and content-targeted native inline source edits, inline footnote reference delimiter syntax-token and metadata/source-edit tests, code-span opening/content/closing syntax-token tests plus code-span content and delimiter marker metadata/source-edit tests, backslash escape marker/escaped-character syntax-token and metadata/source-edit tests, decoded entity source-text syntax-token and metadata/source-edit tests, hard-break marker syntax-token and metadata/source-edit tests for two-space/backslash/HTML spellings, inline and reference-style link target/title plus opening/separator/closing delimiter marker metadata/source-edit tests, image alt/source/title and inline/reference-style image opening/separator/closing delimiter marker source-edit tests, linked-image alt/source/image-title/link-target/link-title plus outer opening/separator/closing delimiter marker metadata/source-edit tests, autolink target and angle marker metadata/source-edit tests, inline HTML tag opening/closing marker metadata plus nested inline source-map coverage, representative semantic-object provenance test covering code fences and semantic fenced blocks | Partial | Add remaining high-risk nested/container provenance golden cases |
| Syntax-to-semantic mapping | `AssociatedObject` plus object-level and selected token-level `SourceSpan` binding | `MarkdownInvariantAssert.MappedAssociatedObjectsAreConsistent`, mixed semantic-object provenance test, final-tree associated-object lookup helpers by line, position, containing span, and overlapping span, table-cell/list-item/definition-list/code-fence/semantic-fence/callout/footnote/sequence-inline-wrapper source-mapping tests, quote/list-item marker token lookup that keeps syntax-only marker nodes separate from semantic child blocks, list-item, quote, callout-body, definition-list group/term/value/body, details-body, footnote-body, table, and table-cell generic syntax-child owner projection, cloned exact child-syntax reuse to avoid parent/root corruption, list-item syntax-only reference-definition preservation during final rebuild, definition-list stale parsed syntax invalidation after public definition-body mutation, inline-normalized callout body syntax invalidation when child markdown changes, nested list-item quote/ordered-list associated-object source remapping test | Partial | Continue hotspot audit for any syntax wrappers or token fields without semantic owners |
| Native snapshot | `MarkdownNativeDocument` and snapshot DTOs for hosts | native document tests, native heading level/text field spans including multiline setext field spans, paragraph payload field spans/source edits, front matter key/value source fields/source edits, image block alt/source/title/link field spans/source edits, table alignment-row field span, exact list/task marker text and source-field/source-edit coverage, list-item content span/source-edit coverage for nested list edits, native list-item, table-cell, and definition-list group/term/definition enumeration/position lookup coverage including list-nested table-cell snapshots and source edits, inline metadata enumeration/position lookup coverage, block source-field enumeration/name-filter/position-lookup/source-edit coverage, reference-definition field enumeration/name-filter/position-lookup/source-edit coverage, repeat-aware block `SourceFields` snapshot coverage that matches live field enumeration, native transform diagnostics with related source spans for individual affected input blocks and precise nested changed-node spans, quote marker/body span/source-edit coverage including nested list-item quote markers, native callout kind/title/body and details summary/body field-span coverage, native definition-list, footnote definition/reference, thematic-break projection plus exact marker source fields, footnote-definition opening/separator marker source fields and snapshots, reference-definition metadata/span tests including opening/separator marker spans and source-field snapshots, raw/comment HTML block source spans and source edits, HTML comment opening/body/closing marker source fields and snapshots, simple raw HTML opening-tag/body/closing-tag source fields and snapshots, processing-instruction, CDATA, and declaration opening-marker/body/closing-marker source fields and snapshots, fenced-code opening/info/content/closing field spans including nested blockquote/list/footnote snapshots, nested ordered-list marker source edits, reference-definition, inline footnote label/delimiter marker, inline hard-break marker spelling/source span, inline/reference-style link target/title and opening/separator/closing delimiter marker spans, autolink target/angle marker metadata spans, inline/reference-style image alt/source/title and opening/separator/closing delimiter marker spans, linked-image metadata and outer opening/separator/closing delimiter marker source-edit preservation tests, parsed formatting/code-span/inline-HTML-tag opening/closing marker source metadata, code-span content source metadata, backslash escape marker/escaped-character source metadata, decoded entity source-text metadata, syntax-aware custom inline projection, nested emphasis inline source-edit preservation, and native-document lossless write helpers for AST-derived edits | Partial | Add more edit/roundtrip fixtures as lossless trivia mode lands |
| Lossless trivia/roundtrip | Early groundwork exists: syntax-backed parse results expose normalized source slices for span-backed nodes, render/write extension contexts can materialize normalized/original source slices directly from token-level `MarkdownSourceSpan` values, `PreserveTrivia` retains raw reader input, line-ending-equivalent original input, including CRLF and standalone CR, can materialize original source slices through line/column coordinates, parsed formatting sequence inlines, parsed code-span syntax tokens plus content/delimiter metadata, backslash escape and decoded-entity syntax tokens plus source metadata, hard-break marker syntax tokens and source metadata, inline footnote reference delimiter syntax tokens, inline/reference-style link/image opening/separator/closing delimiters, linked-image outer opening/separator/closing delimiters, autolink targets/angle markers, and supported inline HTML tag wrappers expose source-backed marker metadata, and `MarkdownRoundtripWriter` can preserve unchanged trivia-backed parse results byte-for-byte or apply explicit native source edits to original input when every edit remaps safely. Transform diagnostics now expose top-level changed-block detection, individual affected input-block spans, deepest original/final syntax-node anchors, precise original child-node anchors for one-to-one single-block transforms, and final-tree nested container body comparisons where syntax fingerprints prove a smaller changed source node; native diagnostics/snapshots plus roundtrip fallback diagnostics prefer the most specific proven source span and preserve related source spans for editor hosts. `MarkdownNativeDocument` now exposes thin write helpers over that writer, including multi-edit coverage that applies shuffled heading/link metadata edits, multiline setext heading text/underline edits, and nested list-item content edits while preserving original CRLF and surrounding trivia. It reports fallback diagnostics for generated or normalized output when trivia is missing, transforms changed the document, original-source mapping is unsafe, or edits overlap; source-edit and transform fallback diagnostics include the affected source span and related affected input spans when available for editor recovery. Full trivia capture, complete delimiter-token capture across all inline spellings/rebalanced delimiter edge cases, full multi-node/generated-node below-block diffing, and general byte-preserving edit writing are not implemented. | `Docs/officeimo.markdown.lossless-roundtrip-design.md`, source-slice tests, roundtrip-writer tests, transform no-op and per-block/deepest-node source-span tests, precise inline and nested transform node-span tests, native transform related-source-span snapshot tests, roundtrip transform related-source-span fallback tests, fenced-code, inline-token, formatting-marker, code-span syntax token, backslash-escape syntax token, decoded-entity syntax token, hard-break marker syntax-token/source-edit tests, inline-footnote marker syntax-token/source-edit tests, inline/reference-style link/image marker source-edit tests, linked-image marker source-edit tests, autolink marker/source-edit tests, inline-HTML-tag marker metadata, nested emphasis inline source-edit preservation tests, code-span content/marker, backslash-escape, decoded-entity source-edit tests, shuffled multi-edit native document roundtrip tests, multiline setext heading token roundtrip tests, and native list-item content roundtrip tests | Partial | Implement parser-owned trivia capture, full original/normalized offset mapping, complete delimiter-token capture, full multi-node/generated-node below-block diffing, and generated-node roundtrip diagnostics before broad editor app claims |

## Known AST Cleanup Targets

| Node | Risk | Direction | Status |
| --- | --- | --- | --- |
| `FootnoteDefinitionBlock` | Medium | Block children are now the primary structured content; `ChildBlocks` is the public AST-style projection over `Blocks`; public structured constructors support list/code/rich body construction; the legacy text constructor now creates a paragraph child-block body so traversal, syntax projection, and transforms see the same semantic shape; paragraph and inline views are derived, with unchanged legacy text preserved as a compatibility view; parsed labels expose token-level source spans and native projection metadata; native footnote projections expose body source spans aggregated from structured child blocks; owned syntax follows the shared canonical child-syntax reconciliation path so exact cached syntax is cloned and stale parsed body syntax is pruned when the canonical block body changes | Partial |
| `ListItem` | High | Paragraph blocks now own list-item paragraph syntax; public `ChildBlocks`, `BlockChildren`, and the child-container interface expose the same mixed AST-style block projection while `Children` remains the nested-block collection; parsed list items expose owned syntax children through the generic syntax-child owner path; rewrite projection is centralized in `ListItem.ReplaceBlockChildren` and preserves parsed source spans when a rewrite keeps the same block projection; owned syntax rebuilding follows the shared canonical child-syntax reconciliation path, preserves syntax-only reference-definition children that are not part of the public semantic block projection, and drops stale cached semantic child syntax when paragraph/nested-block projections change; parsed items expose exact list-marker source text/spans, and parsed task items expose exact task-marker source text/spans through semantic, syntax-tree token nodes, native, snapshot, and block source-field projections | Partial |
| `DefinitionListBlock` | High | Groups/terms/definitions are structured and legacy tuple items are adapters; public `TermItems` exposes first-class semantic term nodes while `Terms` remains a derived inline compatibility view; public `ChildBlocks` exposes definition body blocks as the derived child-container projection; parsed definition lists expose their grouped syntax through the generic syntax-child owner path, clone exact cached group syntax instead of reusing parented parsed nodes, and reconcile cached definition-term/value children against current semantic terms and definition body blocks; the flat `Entries` adapter keeps term replacement synchronized with the owning group and clears parsed syntax caches; native projection exposes groups, semantic terms, definition bodies, nested blocks, and snapshots; group/term/value syntax maps source spans back to semantic group/term/definition objects; native document navigation can enumerate and position-resolve groups, terms, and definition bodies; the parser now also accepts Markdig-style colon-marker definition lists with multiple term lines and multiple definition marker lines while keeping one/two-space marker lines as paragraphs | Partial |
| `TableBlock` | High | Typed `TableCell` model carries row/column metadata, spans, blocks, and syntax ownership, including empty parsed cells with addressable whitespace spans for syntax lookup/native edits; public `TableBlock.ChildBlocks` now exposes the flattened cell-block projection used by the child-container interface, parsed tables expose owned row/header/alignment syntax through the generic syntax-child owner path, each `TableCell` exposes its own block body through the same generic child-container shape, and parsed table cells now expose owned syntax children through the shared canonical child-syntax reconciliation path; continue treating raw/inline views as caches or adapters | Partial |
| `CalloutBlock` | Medium | Title inlines and child blocks are primary for parsed callouts; public structured constructors, the fluent body builder, and the legacy text-body constructor now create child-block bodies so traversal, syntax projection, and transforms see the same semantic shape; body/title text helpers are derived or preserved as compatibility views; parsed kind and explicit title tokens now carry semantic/native source spans; native projections expose body source spans aggregated from structured child blocks; parsed callout bodies now use the shared canonical child-syntax reconciliation path so exact cached body syntax is cloned and stale parsed body syntax is pruned when a rebuilt callout carries a changed body projection | Partial |

## Recent Parity Slices

- 2026-06-28: `UseDefinitionLists` gained Markdig-style colon-marker parsing for the pinned comparison package: `Term` followed by `:   Definition` or `:\tDefinition` now maps to grouped definition-list AST/source syntax, multiple term lines and multiple definition markers stay in one group, and one/two-space marker lines remain paragraphs like Markdig.
- 2026-06-28: `UseAutoIdentifiers` is now promoted to `Covered`: HTML rendering has an explicit `AutoHeadingIdentifiers` switch, Markdig default and GitHub-compatible heading slug styles, duplicate-slug tracking, GFM profile wiring, and focused output tests while existing heading traversal/source metadata remains intact.
- 2026-06-28: `UseSoftlineBreakAsHardlineBreak` is now promoted to `Covered`: `MarkdownReaderOptions.SoftLineBreaksAsHardLineBreaks` parses ordinary paragraph soft breaks as hard breaks while CommonMark/GFM defaults remain soft; focused tests cover HTML output, normalized Markdown writing, nested list paragraph propagation, and native metadata that avoids inventing a fake two-space/backslash marker.
- 2026-06-28: GFM HTML output now has a first-class `HtmlOptions.CreateGitHubFlavoredMarkdownProfile()` that enables cmark-gfm task-list HTML, footnote HTML, raw HTML allow mode, and the GitHub tag filter together. Focused tests keep that spec-output profile separate from strict renderer presets, which continue stripping raw HTML for untrusted hosts.
- 2026-06-28: Extended autolink parsing now exposes Markdig-style previous-character and domain-without-period options. The GFM profile disables domain-without-period bare links while OfficeIMO defaults preserve existing `localhost`/`www.local` behavior, and table-cell inline parsing now carries the same autolink option contract as top-level paragraphs.
- 2026-06-28: Link-label scanning now skips whole supported inline HTML wrapper spans before matching closing brackets or rejecting nested links, so `[outer <u>[inner](https://inner.example)</u>](https://outer.example)` parses as one outer link while the inner link syntax remains literal inside the wrapper.
- 2026-06-28: `UseFootnotes` is now promoted to `Covered`: the GFM footnote corpus covers GitHub HTML shape, first-reference ordering, repeated-reference backrefs, missing/unused definitions, nested block bodies, and source spans; source/native tests cover definition/reference labels and delimiter markers; and structured footnote bodies now write valid GFM, reparse into paragraph/quote/code body blocks, and render GitHub backrefs after final non-paragraph blocks.
- 2026-06-28: `UsePipeTables` is now promoted to `Covered`: the GFM table corpus covers delimiter-row validation, escaped/code-span pipes, body-row padding/truncation, table continuation, and list/blockquote container ownership; table syntax/native tests cover alignment rows and empty cells; and Markdown writing now preserves rendered structured-cell escapes when aligned pipe tables are reparsed through the GFM profile.
- 2026-06-28: `UseTaskLists` is now promoted to `Covered`: checked, unchecked, uppercase, nested, and invalid tight-marker GFM fixtures are paired with semantic AST flags, exact marker source spans, native snapshots/source edits, GitHub HTML rendering, and ordered/unordered Markdown writer roundtrip proof.
- 2026-06-28: GFM table body parsing now continues plain text rows without pipe characters until a blank line or another block-level construct starts, matching the official table extension case where `bar` becomes a single-cell row. The checked-in GFM inventory now includes the seven missing official `test/spec.txt` table-extension examples, moving tracked GFM fixtures from 36 to 43 and table fixtures from 19 to 26.
- 2026-06-28: Link destinations and titles now decode HTML character references after backslash unescaping before URL attribute encoding, adding official CommonMark examples 32, 503, and 506 for inline/reference-shared destination/title parity.
- 2026-06-28: CommonMark type 7 HTML blocks now require only whitespace after the completed non-block tag, keeping inline HTML with trailing content inside paragraph parsing; official CommonMark examples 168, 344, 613, 614, 616, 618, 620, and 623 are now pinned as smoke fixtures, moving the full inventory from 570/652 to 575/652.
- 2026-06-28: Link destination/title parsing now uses CommonMark link whitespace instead of .NET Unicode whitespace, rejects malformed angle destinations before literal fallback, and rejects unescaped matching title delimiters; official CommonMark examples 201, 494, 507, and 508 are now pinned as smoke fixtures, moving the full inventory from 575/652 to 579/652.
- 2026-06-28: HTML block start detection now follows CommonMark's relaxed type 1/type 6 tag-name-prefix rules and treats `textarea` as a raw-text container; official CommonMark examples 153, 154, 156, 157, 158, 171, 172, and 173 are now pinned as smoke fixtures, moving the full inventory from 579/652 to 587/652.
- 2026-06-28: Reference definitions now support multiline titles that open after the destination and close on a later line; official CommonMark example 196 is now pinned as a smoke fixture, moving the full inventory from 587/652 to 588/652.
- 2026-06-28: Reference definitions now support multiline labels whose closing `]:` marker starts a continuation line; official CommonMark example 208 is now pinned as a smoke fixture with separator-marker syntax coverage, moving the full inventory from 588/652 to 589/652.
- 2026-06-28: Reference-definition pre-scan now respects open paragraph text, keeping paragraph-interrupting definition-looking lines literal while preserving later valid definitions; official CommonMark example 213 is now pinned as a smoke fixture with native no-definition coverage, moving the full inventory from 589/652 to 590/652.
- 2026-06-28: Reference-definition pre-scan now discovers blockquote-contained definitions before earlier paragraphs resolve shortcut references, while empty reference-definition-only quotes render as empty blockquotes and native metadata points at the original quoted columns; official CommonMark example 218 is now pinned as a smoke fixture, moving the full inventory from 590/652 to 593/652.
- 2026-06-28: HTML type 7 tag parsing now requires CommonMark tag-name and attribute boundaries, keeping invalid angle autolinks and malformed closing tags in paragraph text while preserving multiline raw-text type 1 starts; official CommonMark examples 602, 606, 609, 610, 617, and 624 are now pinned as smoke fixtures, moving the full inventory from 593/652 to 599/652 and clearing the CommonMark autolink failure bucket.
- 2026-06-28: Raw inline HTML now restores source line breaks from paragraph source maps after invalid angle-link destinations fall back to inline parsing; official CommonMark example 491 is now pinned as a smoke fixture, moving the full inventory from 599/652 to 601/652.
- 2026-06-28: Paragraph joining now preserves raw source line endings only inside matched multiline code spans, so trailing spaces and backslashes remain code content instead of becoming paragraph hard-break markers; official CommonMark examples 336 and 641 are now pinned as smoke fixtures, moving the full inventory from 601/652 to 603/652.
- 2026-06-28: Link parsing now deactivates outer link/reference openers when a label contains a resolved nested link while still allowing image content inside link labels, and unresolved shortcut references inside open inline frames no longer swallow emphasis/link parsing; official CommonMark examples 518, 519, 520, 523, 531, 532, and 533 are now pinned as smoke fixtures, moving the full inventory from 603/652 to 611/652 and reducing the link/reference failure bucket from 15 to 7.
- 2026-06-28: Unresolved shortcut-reference-looking text with backslash escapes now re-enters inline parsing instead of freezing the escaped punctuation in a literal substring; official CommonMark example 545 is now pinned as a smoke fixture, moving the full inventory from 611/652 to 612/652 and reducing the link/reference failure bucket from 7 to 6.
- 2026-06-28: Link-label scanning now skips matched code spans, raw inline HTML tags, and angle autolinks before choosing a closing bracket, so code/raw/autolink text can defeat inline and reference links; official CommonMark examples 524, 525, 526, 536, 537, and 538 are now pinned as smoke fixtures, moving the full inventory from 612/652 to 619/652 and clearing the CommonMark link/reference failure bucket.
- 2026-06-28: CommonMark HTML comparison now treats only HTML-collapsible ASCII whitespace as collapsible text, preserving non-breaking spaces inside code spans and delimiter-looking text. Official CommonMark examples 333 and 334 are now pinned as smoke fixtures, moving the full inventory from 619/652 to 622/652, clearing the CommonMark code-span failure bucket, and reducing the emphasis failure bucket from 9 to 8.
- 2026-06-28: CommonMark raw inline HTML now uses strict tag parsing with multiline attribute whitespace, accepts inline comments, processing instructions, declarations, and CDATA, rejects malformed tags back to text, and disables OfficeIMO blank-line-preserved table/details HTML blocks in strict profiles; the full inventory moved from 622/652 to 632/652, clearing the CommonMark Raw HTML failure bucket and reducing the HTML block/raw HTML cluster from 11 failures to 1.
- 2026-06-28: Blockquote-contained raw HTML blocks now preserve the block boundary line break before the quote closes; official CommonMark example 174 is now pinned as a smoke fixture, moving the full inventory from 632/652 to 633/652 and clearing the HTML block/raw HTML failure bucket.
- 2026-06-28: CommonMark character references now use a shared parser decoder for remaining HTML5 named references and invalid numeric replacement; official CommonMark examples 25 and 26 are now pinned as smoke fixtures, moving the full inventory from 633/652 to 635/652 and clearing the entity decoder failure bucket.
- 2026-06-28: Paragraph joining now preserves hard-break-looking markers inside matched raw inline HTML tags and keeps a trailing backslash literal at the end of a block; official CommonMark examples 642, 643, and 644 are now pinned as smoke fixtures, moving the full inventory from 635/652 to 638/652 and clearing the hard-line-break failure bucket.
- 2026-06-28: CommonMark emphasis delimiter handling now keeps opposite-marker delimiter content inside empty emphasis/strong spans, supports root double-delimiter runs that split into nested italic when only single closers exist, and keeps the mixed-marker strong span in `*foo __bar *baz bim__ bam*`; official CommonMark examples 408, 438, 441, 450, 453, and 470 are now pinned as smoke fixtures, moving the full inventory from 638/652 to 644/652 and reducing the emphasis failure bucket from 8 to 2.
- 2026-06-28: CommonMark same-marker emphasis handling now prefers immediate double closers and nested single-marker opener/closer pairs before splitting a double run into dual italic; official CommonMark examples 418 and 432 are now pinned as smoke fixtures, moving the full inventory from 644/652 to 646/652 and clearing the emphasis failure bucket.
- 2026-06-28: CommonMark container indentation now preserves tab residual columns when stripping nested container indentation, keeps blank lines inside root and list-contained indented code blocks when later indented content continues the block, respects blockquote/code precedence at four indentation columns, splits adjacent blockquotes across unquoted blank lines, and normalizes blockquote inner indentation thresholds. Official CommonMark examples 9, 111, 231, 242, 252, and 264 are now pinned as smoke fixtures, moving the full inventory from 646/652 to 652/652 and clearing the container indentation failure bucket.
- 2026-06-28: URL attribute rendering now percent-encodes unsafe ASCII URL characters such as backslashes and square brackets while preserving existing percent escapes, adding official CommonMark autolink example 603 to the smoke fixture.
- 2026-06-28: Definition-list terms now have first-class `DefinitionListTerm` semantic nodes with syntax/source-span ownership and native projection while the existing `DefinitionListGroup.Terms` inline-sequence API remains as a compatibility view.
- 2026-06-28: Parsed raw HTML declaration blocks now expose source-backed opening marker, body, and closing marker syntax tokens plus native source fields, snapshots, caret lookup, source edits, and a CommonMark declaration smoke fixture while malformed raw HTML remains on the whole-block fallback.
- 2026-06-28: Parsed raw HTML processing-instruction and CDATA blocks now expose source-backed opening marker, body, and closing marker syntax tokens plus native source fields, snapshots, caret lookup, and source edits while malformed raw HTML remains on the whole-block fallback.
- 2026-06-28: Parsed simple raw HTML tag frames now expose source-backed opening tag, body, and closing tag syntax tokens plus native source fields, snapshots, caret lookup, and source edits while malformed raw HTML remains on the whole-block fallback.
- 2026-06-28: Parsed HTML comment blocks now expose source-backed opening marker, body, and closing marker syntax tokens plus native source fields, snapshots, caret lookup, and source edits without broad raw-HTML tag tokenization claims.
- 2026-06-28: Parsed footnote definitions now expose source-backed opening and separator marker syntax tokens plus native source fields, snapshots, caret lookup, and source edits while preserving the existing label/body behavior.
- 2026-06-28: Parsed reference link definitions now expose source-backed opening and separator marker syntax tokens plus native metadata, source-field enumeration, caret lookup, source edits, and snapshot fields around single-line and multiline labels.
- 2026-06-28: Parsed inline footnote references now expose source-backed opening/closing delimiter syntax tokens while native label/marker metadata, source edits, and semantic child traversal remain stable.
- 2026-06-28: Parsed inline/reference links and images now expose source-backed delimiter marker syntax tokens while native metadata/source edits and semantic inline traversal remain stable.
- 2026-06-28: Parsed hard breaks now expose source-backed marker syntax tokens for two-space, backslash, and inline HTML break spellings, while native hard-break metadata/source edits and semantic child traversal remain stable.
- 2026-06-28: Parsed backslash escapes and decoded HTML entities now expose source-backed syntax token children under `InlineText`, while native text metadata/source edits and semantic inline traversal remain stable.
- 2026-06-28: Parsed code spans now expose source-backed opening/content/closing syntax tokens while native code inline metadata and source edits remain stable, giving editor hosts exact caret targets for multi-backtick spans without changing semantic inline traversal.
- 2026-06-28: Parsed formatting wrappers now expose source-backed inline opening/closing marker syntax tokens, so strong/emphasis/strong-emphasis/strikethrough/highlight syntax spans cover full delimiter runs while native inline source edits continue targeting semantic content.
- 2026-06-28: Parsed Setext headings now preserve underline marker runs as first-class syntax token nodes and native `setextUnderlineMarker` source fields through both direct and multiline paragraph-promotion parse paths, while semantic heading-level lookup remains stable.
- 2026-06-28: Parsed ATX headings now preserve opening marker runs as first-class syntax token nodes and native `openingMarker` source fields alongside the semantic heading-level field, keeping level lookup stable while exposing exact marker text and source edits.
- 2026-06-28: Parsed ATX headings now preserve optional closing marker runs as first-class syntax token nodes and native `closingMarker` source fields, giving editor hosts exact source spans and source edits for headings such as `### Title ###` without changing semantic heading text.
- 2026-06-28: Parsed blockquotes now preserve quote markers as first-class syntax token nodes with exact source spans, while semantic quote child blocks remain associated through the existing owned-child projection and native quote marker source fields/source edits stay aligned.
- 2026-06-28: Parsed thematic breaks now preserve exact marker text and spans through first-class syntax token nodes, native block source fields, snapshots, position lookup, and source-edit helpers while keeping semantic Markdown rendering normalized to `---`.
- 2026-06-28: Parsed list items now preserve exact list marker and task marker token text alongside spans, projecting `listMarker` and `taskMarker` through first-class syntax token nodes, native block source fields, snapshots, caret lookup, and source-edit helpers.
- 2026-06-27: Paragraph payloads now surface as native `paragraphText` source fields with snapshot, navigation, and source-edit coverage, giving editor hosts a direct span for ordinary paragraph replacement while preserving broader container-body fields where those are the intended target.
- 2026-06-27: Standalone and linked image blocks now expose native alt/source/title/link source fields, so editor hosts can target image tokens directly through `MarkdownNativeDocument` snapshots, position lookup, and source-edit helpers.
- 2026-06-28: Parsed front matter now exposes opening/closing fence marker spans alongside key/value spans through syntax-tree children, native block fields, snapshots, caret lookup, and source-edit helpers.
- 2026-06-27: Parsed front matter entries now carry key/value source spans and project them into syntax-tree children plus native source fields, including scalar, array, boolean, and literal-block values.
- 2026-06-27: Overlapping source-edit roundtrip diagnostics now preserve both conflicting source spans in `RelatedSourceSpans`, so editor hosts can highlight the full conflict instead of only the later edit.
- 2026-06-27: Quote bodies now surface as native `quoteBody` source fields with snapshot, navigation, and source-edit coverage, sharing the structured container body span aggregation used by callouts, details, and footnotes.
- 2026-06-27: Callout and details bodies now surface as native `calloutBody` and `detailsBody` source fields with snapshot, navigation, and source-edit coverage, using shared child-block source-span aggregation for structured containers.
- 2026-06-27: Footnote definition bodies now surface as native `footnoteBody` source fields with snapshot, navigation, and source-edit coverage, so editor hosts can replace the definition body without targeting the whole footnote block.
- 2026-06-27: Valid indented footnote definition labels now keep parser-owned token source spans through original syntax, final syntax rebuilds, native block source fields, snapshots, and source-edit helpers. This narrows the `FootnoteDefinitionBlock` source-map gap without changing the broader partial status for full footnote/cmark-gfm parity.

## Parity Gates

Before claiming Markdig-class parity, require:

1. Current Markdig package baseline in tests and benchmarks.
2. Full CommonMark `0.31.2` corpus imported or an explicit skipped-case inventory.
3. Broader cmark-gfm corpus coverage for enabled GFM extensions.
4. Compatibility matrix updated for every intentional deviation.
5. Tree invariant and associated-object tests for all canonicalized AST nodes.
6. Benchmarks against stable README/docs/chat corpora with parse, syntax-tree parse, HTML render, allocation, and transform costs. The benchmark harness now covers these lanes; before claiming parity, capture and review current release-mode results.
7. Implement the documented lossless/trivia mode design before editor-grade roundtrip claims.
