namespace OfficeIMO.Excel {
    /// <summary>
    /// Specifies whether a chart series uses the primary or secondary axis.
    /// </summary>
    public enum ExcelChartAxisGroup {
        /// <summary>
        /// Series uses the primary axis group.
        /// </summary>
        Primary,
        /// <summary>
        /// Series uses the secondary axis group.
        /// </summary>
        Secondary
    }
}
