# OfficeIMO.OpenDocument.Odp.Pdf

Bidirectional ODP and PDF conversion without Word or Excel dependencies. PDF to ODP defaults to one rendered PDF page per slide for visual fidelity; use `PdfPowerPointImportOptions.CreateEditableTables()` when editable detected tables are the goal.
