<Window xmlns="https://github.com/avaloniaui"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:loc="using:OfficeIMO.Studio.Infrastructure.Localization"
        xmlns:shell="using:OfficeIMO.Studio.Features.Shell"
        xmlns:workspace="using:OfficeIMO.Studio.Features.Workspace"
        xmlns:workflow="using:OfficeIMO.Studio.Features.Workflows"
        xmlns:home="using:OfficeIMO.Studio.Features.Home"
        xmlns:settings="using:OfficeIMO.Studio.Features.Settings"
        xmlns:ai="using:OfficeIMO.Studio.Features.Assistant"
        x:Class="OfficeIMO.Studio.Features.Shell.MainWindow"
        x:DataType="shell:MainWindowViewModel"
        x:Name="StudioWindow"
        Title="OfficeIMO Studio"
        Icon="avares://OfficeIMO.Studio/Assets/OfficeIMO.Studio.ico"
        Width="1280"
        Height="820"
        MinWidth="960"
        MinHeight="620"
        Background="{DynamicResource StudioWindowBrush}"
        Foreground="{DynamicResource StudioTextBrush}"
        ExtendClientAreaToDecorationsHint="False"
        WindowDecorations="Full"
        ExtendClientAreaTitleBarHeightHint="42"
        DragDrop.AllowDrop="True">
  <Grid RowDefinitions="Auto,*" ColumnDefinitions="116,*">
    <Border Grid.Row="0" Grid.ColumnSpan="2" Background="{DynamicResource StudioPanelBrush}"
            BorderBrush="{DynamicResource StudioBorderBrush}" BorderThickness="0,0,0,1">
      <Grid ColumnDefinitions="*,Auto,Auto" ColumnSpacing="12" Margin="12,0" MinHeight="54">



        <Grid Grid.Column="0" ColumnDefinitions="Auto,*,Auto" Margin="0,6,0,0">
          <Image Source="avares://OfficeIMO.Studio/Assets/OfficeIMO.Studio.png" Width="28" Height="28" Margin="4,0,12,6"
                 AutomationProperties.Name="OfficeIMO Studio" />
          <ListBox x:Name="DocumentTabs" x:DataType="shell:StudioDocumentTabHost"
                   Grid.Column="1" ItemsSource="{Binding Tabs}" SelectedItem="{Binding SelectedTab, Mode=TwoWay}"
                   IsVisible="{Binding HasTabs}" SelectionMode="Single"
                   Background="Transparent" BorderThickness="0"
                   ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Disabled">
            <ListBox.ItemsPanel><ItemsPanelTemplate><StackPanel Orientation="Horizontal" /></ItemsPanelTemplate></ListBox.ItemsPanel>
            <ListBox.Styles>
              <Style Selector="ListBoxItem">
                <Setter Property="Padding" Value="0" />
                <Setter Property="Margin" Value="0,0,4,0" />
                <Setter Property="Background" Value="Transparent" />
              </Style>
              <Style Selector="ListBoxItem:selected /template/ ContentPresenter">
                <Setter Property="Background" Value="Transparent" />
              </Style>
              <Style Selector="Border.documentTab">
                <Setter Property="Background" Value="{DynamicResource StudioWindowBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource StudioBorderBrush}" />
                <Setter Property="BorderThickness" Value="1,1,1,0" />
              </Style>
              <Style Selector="ListBoxItem:selected Border.documentTab">
                <Setter Property="Background" Value="{DynamicResource StudioAccentSoftBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource StudioAccentBrush}" />
                <Setter Property="BorderThickness" Value="1,1,1,2" />
              </Style>
            </ListBox.Styles>
            <ListBox.ItemTemplate>
              <DataTemplate x:DataType="shell:StudioDocumentTabViewModel">
                <Border Classes="documentTab" MinWidth="150" MaxWidth="250" Height="37" Padding="11,4,5,4"
                        CornerRadius="7,7,0,0">
                  <Grid ColumnDefinitions="*,Auto" ColumnSpacing="7">
                    <TextBlock Text="{Binding Title}" FontSize="12" FontWeight="SemiBold"
                               TextTrimming="CharacterEllipsis" VerticalAlignment="Center" />
                    <Button Grid.Column="1" Classes="ghost" Command="{Binding CloseCommand}"
                            Content="×" Padding="4,0" VerticalAlignment="Center"
                            AutomationProperties.Name="{loc:Loc Common.Close}" />
                  </Grid>
                </Border>
              </DataTemplate>
            </ListBox.ItemTemplate>
          </ListBox>
          <Button x:Name="OpenDocumentTabButton" Grid.Column="2" Classes="ghost" x:DataType="shell:StudioDocumentTabHost"
                  Command="{Binding OpenNewTabCommand}" Content="+" FontSize="18" Padding="8,1"
                  AutomationProperties.Name="{loc:Loc MainWindow.OpenAnotherDocument}"
                  AutomationProperties.HelpText="{loc:Loc MainWindow.OpenAnotherDocument}"
                  ToolTip.Tip="{loc:Loc MainWindow.OpenAnotherDocument}" />
        </Grid>

        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
          <ToggleButton Classes="tool" Content="{loc:Loc Assistant.Title}" IsChecked="{Binding IsAssistantVisible, Mode=OneWay}" Command="{Binding ToggleAssistantCommand}" />
          <Button Classes="tool" Click="OnCommandsClick" Content="{loc:Loc Commands.PaletteTitle}" />
          <Button Classes="tool" Click="OnFindClick" IsVisible="{Binding HasDocument}"
                  Content="{loc:Loc MainWindow.Find}" AutomationProperties.Name="{loc:Loc MainWindow.Find}" />
        </StackPanel>
        <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="5" VerticalAlignment="Center">
          <Button Classes="tool" IsVisible="{Binding ShowPdfDocumentControls}" Command="{Binding Commands[SaveAs]}" AutomationProperties.Name="{loc:Loc MainWindow.SaveAs}" Content="{loc:Loc MainWindow.SaveAs}" />
          <Button Classes="tool" Click="OnToggleThemeClick" Width="36" Padding="7"
                  AutomationProperties.Name="{loc:Loc MainWindow.Theme}"
                  AutomationProperties.HelpText="{loc:Loc MainWindow.SwitchLightAndDarkAppearance}"
                  ToolTip.Tip="{loc:Loc MainWindow.SwitchLightAndDarkAppearance}">
            <PathIcon Data="{StaticResource IconTheme}" Width="17" Height="17" />
          </Button>
        </StackPanel>
      </Grid>
    </Border>

    <Border Grid.Row="1" Grid.Column="0" Background="{DynamicResource StudioNavBrush}"
            BorderBrush="{DynamicResource StudioBorderBrush}" BorderThickness="0,0,1,0" Padding="6,10">
      <Grid RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto,*,Auto">
        <ToggleButton Grid.Row="0" Classes="nav"
                     IsChecked="{Binding IsHomeMode, Mode=OneWay}" Command="{Binding Commands[Home]}"
                     AutomationProperties.Name="{loc:Loc MainWindow.Home}">
          <StackPanel Spacing="3" HorizontalAlignment="Center"><PathIcon Data="{StaticResource IconHome}" Width="18" Height="18" /><TextBlock Text="{loc:Loc MainWindow.Home}" FontSize="12" Width="96" MaxLines="2" TextWrapping="Wrap" TextAlignment="Center" /></StackPanel>
        </ToggleButton>
        <ToggleButton Grid.Row="1" Classes="nav" IsEnabled="{Binding HasDocument}"
                     IsChecked="{Binding IsPdfWorkspaceMode, Mode=OneWay}" Command="{Binding ShowPdfWorkspaceCommand}"
                     AutomationProperties.Name="{loc:Loc MainWindow.Document}">
          <StackPanel Spacing="3" HorizontalAlignment="Center"><PathIcon Data="{StaticResource IconDocument}" Width="18" Height="18" /><TextBlock Text="{loc:Loc MainWindow.Document}" FontSize="12" Width="96" MaxLines="2" TextWrapping="Wrap" TextAlignment="Center" /></StackPanel>
        </ToggleButton>
        <ToggleButton Grid.Row="2" Classes="nav"
                     IsChecked="{Binding IsToolsMode, Mode=OneWay}" Command="{Binding Commands[Tools]}"
                     AutomationProperties.Name="{loc:Loc MainWindow.Tools}">
          <StackPanel Spacing="3" HorizontalAlignment="Center"><PathIcon Data="{StaticResource IconTools}" Width="18" Height="18" /><TextBlock Text="{loc:Loc MainWindow.Tools}" FontSize="12" Width="96" MaxLines="2" TextWrapping="Wrap" TextAlignment="Center" /></StackPanel>
        </ToggleButton>
        <ToggleButton Grid.Row="3" Classes="nav"
                     IsChecked="{Binding IsOcrMode, Mode=OneWay}" Command="{Binding Commands[Ocr]}"
                     AutomationProperties.Name="{loc:Loc MainWindow.OCR}">
          <StackPanel Spacing="3" HorizontalAlignment="Center"><PathIcon Data="{StaticResource IconOcr}" Width="18" Height="18" /><TextBlock Text="{loc:Loc MainWindow.OCR}" FontSize="12" Width="96" MaxLines="2" TextWrapping="Wrap" TextAlignment="Center" /></StackPanel>
        </ToggleButton>
        <ToggleButton Grid.Row="4" Classes="nav"
                     IsChecked="{Binding IsConversionMode, Mode=OneWay}" Command="{Binding Commands[Convert]}"
                     AutomationProperties.Name="{loc:Loc MainWindow.Convert}">
          <StackPanel Spacing="3" HorizontalAlignment="Center"><PathIcon Data="{StaticResource IconConvert}" Width="18" Height="18" /><TextBlock Text="{loc:Loc MainWindow.Convert}" FontSize="12" Width="96" MaxLines="2" TextWrapping="Wrap" TextAlignment="Center" /></StackPanel>
        </ToggleButton>
        <ToggleButton Grid.Row="5" Classes="nav" IsChecked="{Binding IsJobsMode, Mode=OneWay}" Command="{Binding Commands[Jobs]}"
                      AutomationProperties.Name="{loc:Loc Jobs.Title}">
          <StackPanel Spacing="3" HorizontalAlignment="Center"><PathIcon Data="{StaticResource IconJobs}" Width="18" Height="18" /><TextBlock Text="{loc:Loc Jobs.Title}" FontSize="12" Width="96" MaxLines="2" TextWrapping="Wrap" TextAlignment="Center" /></StackPanel>
        </ToggleButton>
        <ToggleButton Grid.Row="7" Classes="nav"
                      IsChecked="{Binding IsSettingsMode, Mode=OneWay}" Command="{Binding Commands[Settings]}"
                      AutomationProperties.Name="{loc:Loc Settings.Settings}" ToolTip.Tip="{loc:Loc Settings.Settings}">
          <StackPanel Spacing="3" HorizontalAlignment="Center"><PathIcon Data="{StaticResource IconSettings}" Width="18" Height="18" /><TextBlock Text="{loc:Loc Settings.Settings}" FontSize="12" Width="96" MaxLines="2" TextWrapping="Wrap" TextAlignment="Center" /></StackPanel>
        </ToggleButton>
      </Grid>
    </Border>

    <SplitView x:Name="AssistantHost" Grid.Row="1" Grid.Column="1" PanePlacement="Right" OpenPaneLength="400"
               IsPaneOpen="{Binding IsAssistantVisible, Mode=TwoWay}" DisplayMode="Overlay">
      <SplitView.Pane><Border IsVisible="{Binding IsAssistantVisible}"><ai:AssistantView DataContext="{Binding Assistant}" /></Border></SplitView.Pane>
    <Grid RowDefinitions="Auto,*">
      <StackPanel Grid.Row="0">
        <Border IsVisible="{Binding HasVisibleError}" Background="{DynamicResource StudioErrorSoftBrush}"
                BorderBrush="{DynamicResource StudioErrorBrush}" BorderThickness="0,0,0,1" Padding="12,8">
          <Grid ColumnDefinitions="*,Auto"><TextBlock Text="{Binding ErrorMessage}" Foreground="{DynamicResource StudioErrorBrush}" TextWrapping="Wrap" /><Button Grid.Column="1" Classes="ghost" Command="{Binding DismissErrorCommand}" AutomationProperties.Name="{loc:Loc MainWindow.Dismiss}" Content="{loc:Loc MainWindow.Dismiss}" /></Grid>
        </Border>
        <Border IsVisible="{Binding HasSecurityWarning}" Background="{DynamicResource StudioWarningSoftBrush}"
                BorderBrush="{DynamicResource StudioWarningBrush}" BorderThickness="0,0,0,1" Padding="12,8">
          <TextBlock Text="{Binding SecurityWarning}" Foreground="{DynamicResource StudioWarningBrush}" TextWrapping="Wrap" />
        </Border>
        <Border IsVisible="{Binding Session.HasStorageError}" Background="{DynamicResource StudioWarningSoftBrush}"
                BorderBrush="{DynamicResource StudioWarningBrush}" BorderThickness="0,0,0,1" Padding="12,8">
          <Grid ColumnDefinitions="*,Auto" ColumnSpacing="12">
            <TextBlock Text="{Binding Session.StorageError}" Foreground="{DynamicResource StudioWarningBrush}" TextWrapping="Wrap" VerticalAlignment="Center" />
            <Button Grid.Column="1" Classes="tool" Command="{Binding Session.RetryStorageCommand}" Content="{loc:Loc Session.RetryStorage}" />
          </Grid>
        </Border>
        <Border IsVisible="{Binding HasVisibleOperationStatus}" Background="{DynamicResource StudioAccentSoftBrush}"
                BorderBrush="{DynamicResource StudioBorderBrush}" BorderThickness="0,0,0,1" Padding="12,7">
          <Grid ColumnDefinitions="*,Auto,Auto,Auto,Auto">
            <TextBlock TextWrapping="Wrap" Text="{Binding OperationStatus}" Foreground="{DynamicResource StudioAccentBrush}" />
            <Button Grid.Column="1" Classes="tool" AutomationProperties.Name="{loc:Loc MainWindow.RestoreRecoveredEdits}" Content="{loc:Loc MainWindow.RestoreRecoveredEdits}" IsVisible="{Binding HasRecovery}" Command="{Binding RestoreRecoveryCommand}" />
            <Button Grid.Column="2" Classes="tool" AutomationProperties.Name="{loc:Loc MainWindow.Discard}" Content="{loc:Loc MainWindow.Discard}" IsVisible="{Binding HasRecovery}" Command="{Binding DiscardRecoveryCommand}" Margin="6,0" />
            <ProgressBar Grid.Column="3" IsVisible="{Binding IsWorkspaceBusy}" Width="100" Minimum="0" Maximum="1" Value="{Binding OperationProgressFraction}" />
            <Button Grid.Column="4" Classes="tool" AutomationProperties.Name="{loc:Loc MainWindow.Cancel}" Content="{loc:Loc MainWindow.Cancel}" IsVisible="{Binding CanCancelOperation}" Command="{Binding CancelOperationCommand}" Margin="8,0,0,0" />
          </Grid>
        </Border>
      </StackPanel>

      <home:HomeView Grid.Row="1" IsVisible="{Binding IsHomeMode}" />
      <home:ToolsView Grid.Row="1" IsVisible="{Binding IsToolsMode}" />
      <TabControl Grid.Row="1" IsVisible="{Binding IsOcrMode}">
        <TabItem Header="{loc:Loc OcrSession.SinglePdf}"><workflow:SearchablePdfOcrView /></TabItem>
        <TabItem Header="{loc:Loc OcrSession.FilesTab}"><workflow:OcrSessionView DataContext="{Binding OcrSession}" /></TabItem>
      </TabControl>
      <workspace:DocumentWorkspaceView x:Name="DocumentWorkspace" Grid.Row="1" IsVisible="{Binding IsPdfWorkspaceMode}" />
      <Grid Grid.Row="1" IsVisible="{Binding IsConversionMode}" RowDefinitions="Auto,*">
        <Border Padding="20,16,20,10" Background="{DynamicResource StudioPanelBrush}"><StackPanel Spacing="4"><TextBlock Text="{loc:Loc MainWindow.Convert}" Classes="pageTitle" /><TextBlock Text="{loc:Loc MainWindow.PrepareRunAndInspectConversionJobs}" Classes="muted" /></StackPanel></Border>
        <workflow:ConversionWorkbenchView x:Name="ConversionView" Grid.Row="1" />
      </Grid>
      <workflow:OutputIntakeWorkbenchView Grid.Row="1" IsVisible="{Binding IsOutputMode}" />
      <Grid Grid.Row="1" IsVisible="{Binding IsJobsMode}"><workflow:StudioJobsView DataContext="{Binding Jobs}" /></Grid>
      <workflow:DocumentHealthView x:Name="DocumentHealthView" Grid.Row="1" IsVisible="{Binding IsDocumentHealthMode}" />
      <Grid Grid.Row="1" IsVisible="{Binding IsSettingsMode}">
        <settings:SettingsView DataContext="{Binding Settings}" />
      </Grid>
    </Grid>
    </SplitView>
  </Grid>
</Window>
