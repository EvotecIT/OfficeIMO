namespace OfficeIMO.Word {
    /// <summary>
    /// Horizontal alignment options for paragraphs.
    /// </summary>
    public enum HorizontalAlignment {
        Left,
        Center,
        Right,
        Justified
    }
}
