namespace OfficeIMO.Html;

/// <summary>Hover capability exposed to CSS media queries.</summary>
public enum HtmlHoverCapability {
    /// <summary>The static output has no hover state.</summary>
    None,
    /// <summary>The selected environment supports a convenient hover state.</summary>
    Hover
}
