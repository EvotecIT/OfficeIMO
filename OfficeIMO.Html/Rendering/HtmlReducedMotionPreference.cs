namespace OfficeIMO.Html;

/// <summary>Motion preference exposed to CSS media queries during static rendering.</summary>
public enum HtmlReducedMotionPreference {
    /// <summary>No reduced-motion preference is requested.</summary>
    NoPreference,
    /// <summary>Reduced-motion rules are preferred.</summary>
    Reduce
}
