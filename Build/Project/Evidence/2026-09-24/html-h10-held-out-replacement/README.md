# H10 replacement held-out gallery: first capture

The [H10 selection manifest](../../../../../OfficeIMO.Pdf.Benchmarks.Comparisons/Corpus/html-h10-page-selection.json) chose the [Yellowstone Photo Galleries page](https://www.nps.gov/yell/learn/photosmultimedia/photogallery.htm) on 2026-09-24, before any OfficeIMO rendering of it. NASA Voyager became a development case after its failures informed fixes. This NPS page uses a different site and stylesheet system and remains held out. The [NPS disclaimer](https://www.nps.gov/aboutus/disclaimer.htm) says NPS-created material is generally public domain, with exceptions for third-party content and protected marks; the archive is retained only in ignored local evidence.

The capture at `33d30b6369a982e27b64bafb89e5c2e6faf065a0` used an 816 × 900 CSS-pixel viewport, `DOMContentLoaded` plus 1000 ms, and offline Chromium replay without network access. Its MHTML SHA-256 is `cdb13122fa27b71712eee83a4df5a0bb2ff5a675265ce265199e492fe732fd0b` (5,758,212 bytes, 46 resources, no MIME diagnostics). The archived HTML contains all three predeclared markers. Chromium `151.0.7922.34` produced four screen-media pages and four print pages from the archive. The exact capture report and bytes are under `Ignore/HtmlUnknownPageQualification/h10-nps-yellowstone-capture-33d30b636/`.

At source `5237a986256ccfaf53ea91731a6f7f28db44e105`, all six OfficeIMO PDF operations stopped with `CssRuleLimitExceeded`: `MaxCssRules`, actual 10,001, limit 10,000. The archive contains a 2.67 MB site stylesheet with about 17,953 CSS blocks. This is a bounded safety-policy limit on a real unfamiliar page, not a rendering result. PeachPDF `0.9.19` independently failed with `Invalid length` while applying CSS `text-indent`; that failure does not make the OfficeIMO limit acceptable. The exact-head replay report is under `Ignore/HtmlUnknownPageQualification/h10-nps-yellowstone-limit-5237a9862/`.

Do not replace this held-out source because it failed. Before raising the untrusted CSS rule cap, measure parser, selector, memory and time costs on this unchanged archive across supported platforms and retain a bounded budget. PDF visual comparison and editable projections are unavailable until the input passes that gate. The frozen WAI, MDN, NASA and TodoMVC development gaps and the broader H10 acceptance criteria remain in [the roadmap](../../../../../Docs/ROADMAP.md#unfamiliar-page-conversion-qualification).
