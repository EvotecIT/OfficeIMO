# H10 CDC drinking-water article: held-out selection

The [H10 selection manifest](../../../../../OfficeIMO.Pdf.Benchmarks.Comparisons/Corpus/html-h10-page-selection.json) predeclares [Global Drinking Water](https://www.cdc.gov/global-water-sanitation-hygiene/about/global-drinking-water.html) as the next independent held-out page on 2026-09-25, before OfficeIMO capture, rendering or conversion. The USGS atmosphere selection could not be acquired under the declared Chromium workflow because navigation returned HTTP 403; it produced no archive. The CDC page was reachable over HTTPS during source selection and supplies explanatory prose, an article picture, related links and a five-row service-level definition table on a different publisher and stylesheet system from the development cases.

The frozen source must visibly contain “Global Drinking Water”, “Why it's important”, “Terms to know” and “Safely managed water service level”. Capture at 816 × 900 CSS pixels after DOMContentLoaded plus 1000 ms, then replay the exact MHTML without network access. Record archive hash, resource/font inventory, exclusions and browser reference before OfficeIMO conversion. Keep archive bytes and third-party visuals in ignored local evidence.

At clean source `10430e79b`, the declared Chromium workflow captured 1,127,284 MHTML bytes (SHA-256 `879b46d271aaec2625e837fbaef1d900b317806fad268a661f731ffdcd4798c9`), 12 archived resources and no MIME diagnostics. Offline Chromium 151.0.7922.34 produced four screen-media pages and two print pages. The exact archive, browser screenshot/PDFs, OfficeIMO PDFs, PeachPDF 0.9.19 PDF and schema-2 report are under `Ignore/HtmlUnknownPageQualification/h10-cdc-drinking-water-capture-10430e79b/`. The archived HTML contains all four required markers and a div-based ARIA table with a header row and five paired term/definition rows, rather than a native `<table>`.

At the first-pass head, all three OfficeIMO PDF intents completed, but none was qualified. Print produced three pages against Chromium's two, including a blank trailing page; the OfficeIMO screen-media output produced four pages with different table flow. Screen-snapshot pagination produced five 900px viewport slices for the 4,090px browser screenshot; Chromium's separate A4 screen PDF had four pages, so those counts do not measure the same page boundary. The contact-sheet inspection found the article, picture, prose and table visible, but reading-size, page-boundary and geometry acceptance remained open. Print conversion reported 61 diagnostics, including 35 unavailable resources and 22 unavailable font faces; screen intents reported 94 each, including unsupported positioning and private-use glyph omissions. PeachPDF produced three print pages with a visibly displaced/cropped first-page header. These are distinct failure modes, not a parity claim.

The first editable pass saved/reopened all six predeclared targets with all four markers, but Excel initially flattened the role table into one column without a table-specific loss report. The shared ARIA-table projection fix at `97a5a9db8` makes the real saved Excel workbook contain an editable A1:B6 grid: header cells `Term`/`Definition` and five term/definition pairs on the same rows. Native and ARIA row/column spans share the same bounded import path.

The PowerPoint adapter now imports this ARIA table as a native six-row, two-column table. Its first 300-point layout clipped most rows; the table-flow pass uses the available slide width, measures rows at their imported font sizes, and starts the table on a continuation slide. The saved/reopened 20-slide deck retains all four markers. LibreOffice rendered page 13 with all five service-level pairs visible inside the slide. The earlier measured-table deck, rendered PDF, reading-size image and operation report are under `Ignore/HtmlUnknownPageQualification/h10-cdc-drinking-water-editable-probe/powerpoint-measured-rows-candidate/`. Generic tables too tall for one slide fall back to paginated editable text with an explicit structure-loss report; that fallback is not native-table preservation.

The generic adapter also gave repeated document titles a fixed 44-point editable text box. Six CDC slides consequently reported measured text clipping, although LibreOffice painted the two-line title beyond that box. The title box now takes a measured height. Replaying the unchanged archive produced a saved/reopened 20-slide deck with all four markers, eight pictures and three linked pictures. Its report has 16 diagnostics: four pending external stylesheets, two complex-shaping limits and ten approximations, with no text-clipping omissions. LibreOffice again rendered 20 slides; inspected title-bearing slides and the five-pair table slide have no visible overlap or cropped text. The native deck, operation report, PDF and inspected slide rasters are under `Ignore/HtmlUnknownPageQualification/h10-cdc-ppt-title-candidate-20260925/`.

The title-height regression failed against the former 44-point box and passes with measured sizing. The full HTML suite passed 3,416/3,416 tests on both .NET 10 and .NET 8 for this candidate. This verifies the adapter contract and the CDC deck replay; it does not close the remaining visual or resource gaps.

The Word adapter now maps supported ARIA row groups, rows and cells through its native table importer. The frozen CDC archive saved and reopened as a DOCX with one editable six-row, two-column table containing the header and all five term/definition pairs. Its LibreOffice rendering shows the table on pages 6–7 of a 20-page document; the first data row begins on page 6 and the remaining rows are legible on page 7. The source hash and all four text markers matched, and the operation succeeded with 30 diagnostics, including skipped stylesheet links and unsupported CSS declarations; the role-table structure itself was not reported as approximated. The DOCX, report, reopened HTML, rendered PDF and table-page images are under `Ignore/HtmlUnknownPageQualification/h10-cdc-drinking-water-editable-probe/word-role-table-final/`. This proves editable structure and a visible table, not browser-equivalent pagination: browser-hidden navigation still precedes the article, and a blank continuation band appears at the top of page 7. Unsupported ARIA table structures remain in flow with an approximation diagnostic.

The current OneNote and RTF conversions save and reopen the ARIA table as one native six-row, two-column table, with all four required text markers. The exact frozen archive SHA-256 remains `879b46d271aaec2625e837fbaef1d900b317806fad268a661f731ffdcd4798c9`. Both conversions report four applicable stylesheet links as omissions; disabled, alternate and non-CSS stylesheets do not generate false loss reports in focused tests. LibreOffice renders the RTF as ten pages, with the narrow but readable table across pages 5–6 and one row split at that boundary. The final RTF artifacts are under `Ignore/HtmlUnknownPageQualification/h10-cdc-role-table-final/rtf/`, and the OneNote reopen and report are under `Ignore/HtmlUnknownPageQualification/h10-cdc-onenote-stylesheet-final/`. Target-specific acceptance remains open: inspect OneNote appearance, picture and link preservation, broader unsupported CSS-property reporting, PowerPoint's other slides and picture placement/credit, and Markdown's remaining raw HTML-dependent content. The first-pass outputs remain under `Ignore/HtmlUnknownPageQualification/h10-cdc-drinking-water-editable-probe/`.

The saved CDC `.one` also exposes a whitespace-only link label that previously made its semantic PDF export throw in the shared Markdown-to-PDF bridge. Both paragraph and table link paths now use the nonblank destination as the label and annotation fallback. The reopened section exports a six-page semantic PDF with extractable article text and a six-page image-backed visual PDF. Representative page rasters were inspected under `Ignore/HtmlUnknownPageQualification/h10-cdc-onenote-visual-final/`; the article photograph appears in the visual route, but the long service-level table is squeezed into a narrow column. This proves both exports complete, not that OneNote appearance or link/picture fidelity matches the source page.

The narrow table came from native writing's fallback of one half-inch unit per imported column: the earlier saved `.one` reopened with widths `1,1`. The OneNote HTML adapter now assigns bounded content-based widths where supported authored table and cell widths are absent. Replaying the same frozen archive, the saved file reopens with widths about `5.3,9.7` half-inch units. The visual PDF's table page shortened from 4,384 to 2,529 points, and inspection at reading size found all five term/definition pairs visible across the page. The regenerated `.one`, visual PDF and inspected page raster are under `Ignore/HtmlUnknownPageQualification/h10-cdc-onenote-width-final/`. Native-reopen tests cover percentage table width and cell proportions; oversized widths and unprojected `col`/`colgroup` definitions receive approximation reports. This repairs the CDC table's readable width; unprojected column-group geometry, broader unsupported-style reporting and whole-page OneNote fidelity remain open.

A native-content audit of that saved `.one` found eight embedded image payloads, including the article photograph with the same 306,444-byte payload and SHA-256 `c8626edcd249507fb12b2d4f4732f759d7f18df05dae5da6c1d415c1b81e494f` as the archived source. It also found 56 linked text runs, but all three linked CDC logo images had lost their hyperlink. The shared semantic resource now carries its normalized enclosing anchor target, and the OneNote importer writes it to the image. A replay of the unchanged frozen archive saved and reopened eight images, three CDC logo links, 56 linked text runs and the identical article photograph hash; the operation still reports four omitted external stylesheets. The repaired native artifact and audit probe are under `Ignore/HtmlUnknownPageQualification/h10-cdc-onenote-width-final/`. PowerPoint's generic and semantic picture paths now retain absolute image links and resolvable slide fragments; Excel's generic and semantic picture paths report unsupported image links as omissions. Replaying the same CDC archive, the reopened PowerPoint file has eight pictures, three linked to `https://www.cdc.gov/`, and 22 diagnostics with no image-link loss. The reopened Excel file retains all four selected text markers and reports three image-link omissions among 19 diagnostics. Both reports and native files are under `Ignore/HtmlUnknownPageQualification/h10-cdc-drinking-water-editable-probe/` in their `powerpoint-image-links-final` and `excel-image-links-final` subdirectories. Visible slide and sheet presentation, OneNote's whole-page appearance and visual-PDF link behavior remain unqualified.

At clean code head `8b2818106`, H4 advanced-held-out acceptance passed all eight cases with no runner failures; its report is under `Ignore/HtmlUnknownPageQualification/h10-h4-linked-images-8b2818106/`. The final .NET 8 HTML suite passed 3,407/3,407 tests. The .NET 10 suite passed 3,405/3,405 before the two review-driven cases were added, and all five affected image-link cases passed after their fixes. OneNote passed 342/342 on both .NET 8 and .NET 10, and the Excel, PowerPoint and OneNote HTML adapters built for netstandard2.0 without warnings. The local runs used installed SDK 10.0.303 because the repository-pinned 10.0.112 SDK was unavailable on this host.

At clean code head `9e7a7937d`, the OneNote suite passed 341/341 tests on net10 and net8, the full HTML suite passed 3,401/3,401 on both frameworks, the OneNote HTML adapter built for netstandard2.0, and H4 advanced-held-out acceptance passed 8/8 with no runner failures. The H4 report is under `Ignore/HtmlUnknownPageQualification/h10-h4-onenote-width-9e7a7937d/`. This does not establish CDC-specific time or memory budgets on the supported platforms.

The final ARIA-table code head passed 3,368/3,368 HTML tests on both net10 and net8, the Excel adapter's netstandard2.0 build, and H4 advanced-held-out acceptance 8/8. The macOS H4 static budget first failed only a 752 MB sampled peak against its 700 MB ceiling; an idle repeat at the same clean head passed at 514 MB with deterministic cold/warm fingerprints. Both reports are retained; a single passing repeat does not explain the earlier peak. CDC-specific cost budgets on all supported platforms and the remaining PDF/editable acceptance criteria stay open.

At clean code head `f6df951d4`, Markdown conversion maps supported ARIA role tables through the shared structural normalizer. The unchanged CDC archive now saves and reloads a native Markdown table with two columns and all five service-level term/definition pairs; the earlier artifact flattened those pairs into alternating paragraphs. The 474,233-byte artifact retains all four selected text markers. Its operation report still declares loss for two raw HTML blocks and 17 raw HTML inlines elsewhere in the document, so this table repair does not qualify the whole Markdown output. The final artifact and report are under `Ignore/HtmlUnknownPageQualification/h10-cdc-drinking-water-editable-probe/markdown-role-table-final/`.

Independent review caught a first-candidate regression where unwrapping link and image role-cell hosts would lose their content. The normalizer now retains those hosts, with focused save/reparse coverage. The full Markdown suite passed 3,001/3,001 tests on net10 and net8; the affected HTML role-table tests passed 13/13, the Markdown HTML adapter built for netstandard2.0 without warnings, and H4 advanced-held-out acceptance passed 8/8 at the clean code head. H4 evidence is under `Ignore/HtmlUnknownPageQualification/h10-h4-markdown-aria-f6df951d4/`. The macOS Markdown probe took 436 ms and allocated 96.1 MB cumulatively; this is not a supported-platform cost budget.

The Word role-table checkpoint passed 3,382/3,382 HTML tests on both net10 and net8, including regressions for selector styling, links/images carried by role cells, source limits, relative font units and native-tag rules that must not apply to synthetic cells. A read-only local review found CSS and host-content regressions in the first candidate; both were fixed before the saved CDC artifact and full suites were rerun.

At clean head `72e56f066`, the renderer resolves flex child percentages against the container's content width and floors border-box flex items at their padding and border size. The latter matters for this page's full-width key-points header followed by a zero-basis, padded text column. The frozen archive hash remained `879b46d271aaec2625e837fbaef1d900b317806fad268a661f731ffdcd4798c9`. The offline replay under `Ignore/HtmlUnknownPageQualification/h10-cdc-flex-clean-72e56f066/` has no runner failures: OfficeIMO and Chromium print both have two pages, while OfficeIMO screen-media has four, screen-snapshot has five and Chromium screen has four. PeachPDF still has three print pages. The full HTML suite passed 3,385/3,385 tests on net10 and net8; a subsequent focused column-flex sibling test also passed. H4 advanced-held-out acceptance passed all eight cases at the same clean head, with output under `Ignore/HtmlUnknownPageQualification/h4-flex-padding-72e56f066/`.

All final CDC print and screen pages were inspected as page rasters. The blank OfficeIMO print page is gone, and the key-points text and photo share a row in print and screen output. At the flex checkpoint, print still exposed skip links hidden by Chromium and used larger text and looser spacing: OfficeIMO page one ended at “Water kiosks” while Chromium reached “Unprotected spring”; both retained the table and sources on page two. Screen-media still differed at article/table boundaries despite matching Chromium's four-page count. Screen-snapshot placed the CDC footer across the last two viewport slices; the frozen browser screenshot also requires five 900px slices. The footer content and geometry still differ at reading size. The replay reported 61 print and 94 screen-intent diagnostics, including unavailable resources and font faces. Page-count repair did not qualify any PDF intent, the editable targets or CDC-specific cross-platform cost budgets.

At clean head `9beb2bd4f`, paged root-positioned boxes wholly above or left of the initial page area are omitted. Browser print probes confirmed that partially visible boxes, transformed boxes moved into view, and right/bottom margin overflow must remain; a positioned child extending into the page remains visible. The frozen CDC archive hash is unchanged. Its offline replay under `Ignore/HtmlUnknownPageQualification/h10-cdc-position-final-9beb2bd4f/` reports no failures: OfficeIMO and Chromium print both have two pages, OfficeIMO screen-media and Chromium screen have four, OfficeIMO screen-snapshot has five, and PeachPDF print has three. The OfficeIMO print output no longer contains the two skip-link labels; the remaining print text is unchanged from the flex checkpoint. Print still has 61 diagnostics and screen intents have 94 each. The focused positioning regressions pass on net10 and net8, the full HTML suite passed 3,387/3,387 on both frameworks before the final test-only addition, and H4 advanced-held-out acceptance passed all eight cases at the clean code head. The print PDF still differs in text density and page flow, while screen pagination, missing resources and font faces, editable target checks, and CDC-specific cross-platform budgets remain open.

The macOS H4 static budget run at clean documentation head `b9ff97ca7` is retained under `Ignore/HtmlUnknownPageQualification/h4-static-budget-b9ff97ca7/`. It failed only warm elapsed time: 4.33–4.61 seconds against the 3.0-second ceiling. Cold time, allocations, output size, peak memory, cancellation and deterministic fingerprints met their limits. The output fingerprint exactly matches the earlier passing `97a5a9db8` run, and warm allocations stayed near 856 MB. An unrelated Xcode build was consuming substantial CPU during this run, so timing needs an idle-host repeat before attributing the failure to the renderer or declaring this budget passed.

At clean code head `afa2cba6d`, generic two-column Excel tables receive bounded column widths and wrapping on cells that were actually imported. The frozen CDC archive hash remained `879b46d271aaec2625e837fbaef1d900b317806fad268a661f731ffdcd4798c9`. The reopened workbook retained all four selected text markers and its 19 diagnostics, including three unsupported image-link omissions. LibreOffice rendered the workbook as 14 pages; its first page shows the native six-row service-level table with all five term/definition pairs readable. The workbook, report, PDF and inspected first-page raster are under `Ignore/HtmlUnknownPageQualification/h10-cdc-excel-readable-afa2cba6d/`. An earlier 15-page export clipped the definitions horizontally. Saved-workbook tests also cover empty source rows and a `MaxTableCells` truncation without creating omitted cells. The full HTML suite passed 3,415/3,415 on both net10 and net8, the Excel HTML adapter built for netstandard2.0 without warnings, and H4 advanced-held-out acceptance passed 8/8 with no runner failures at this clean head. The macOS H4 time budget remains unqualified while unrelated Xcode compilation consumes the host.

The PowerPoint deck was inspected as a 20-slide LibreOffice PDF, including all-slide contact sheets and reading-size slides 9 and 13. Slide 13 keeps the complete service-level table readable; slide 9 has substantial empty space, and browser-hidden navigation precedes the article. The measured-title replay removes the six text-clipping omissions but does not qualify the whole editable deck or CDC page fidelity.

At source `29d492312`, a separate opt-in CSS visibility preparation for editable import applies archived stylesheets only to select visible content for the requested media. It removes subtrees with computed `display:none` or zero opacity, then discards the temporary CSS so the target import remains bounded and still reports unsupported styling. The unchanged CDC archive (SHA-256 `879b46d271aaec2625e837fbaef1d900b317806fad268a661f731ffdcd4798c9`) applied five archived stylesheets and omitted 20 hidden subtrees in the print-oriented Word path. The saved/reopened DOCX retains all four predeclared article markers. LibreOffice renders it as six pages, with the article starting on page one; the earlier unfiltered DOCX rendered as 20 pages with article content starting on page six. The final DOCX, reopened HTML, report, PDF and inspected first-page raster are under `Ignore/HtmlUnknownPageQualification/h10-cdc-style-probe/word-clean-29d492312/`.

This preparation and Word conversion succeeded with 45 combined diagnostics, including 32 unavailable CSS dependencies and three stylesheet links skipped by the target. The first page still contains two skip links; its large logo, QR placement, missing callout image and text spacing differ substantially from the frozen Chromium print reference. The six editable Word pages are not a failed comparison against Chromium's two-page print PDF because these are different output contracts, but the visible differences remain target-specific acceptance gaps. Image and stylesheet preparation currently enforce separate resource budgets; a combined operation-level budget, other editable targets, off-page skip links and cross-platform CDC cost budgets remain open. The local macOS probe took 8.65 seconds and allocated 1.10 GB cumulatively, which is measurement context rather than a qualified budget.

The focused MHTML class passed 11/11 on .NET 8 and .NET 10, the full HTML suite passed 3,425/3,425 on both frameworks, the MHTML netstandard2.0 build had no warnings, and the clean-source H4 advanced held-out gate passed 8/8 with no failures under `Ignore/HtmlUnknownPageQualification/h10-h4-visible-content-29d492312/`. One concurrent .NET 8 full-suite run failed an existing 100 ms serialization-deadline test while unrelated simulator processes heavily loaded this Mac; both cases passed alone and the subsequent .NET 8 full-suite rerun passed. Independent review found and confirmed fixes for hidden document roots, serialized shadow-root projection and blocked-stylesheet loss reporting.

At source `d30ea734a`, the same frozen archive was replayed with the archived CSS visibility preparation and bounded Word image sizing. Five archived stylesheets were applied and 20 hidden subtrees omitted. The saved/reopened DOCX retains all four selected markers; LibreOffice renders four letter pages with the article on page one, compared with six pages in the previous visible-content run. The operation succeeds with declared loss and 47 combined diagnostics, including one image-fit approximation. The report, DOCX, rendered PDF and inspected first-page raster are under `Ignore/HtmlUnknownPageQualification/h10-cdc-style-probe/word-intrinsic-final/`. The remaining skip links, header imagery, missing callout art and spacing still differ from the browser reference; target-specific visual acceptance and combined resource budgets remain open. The final HTML suite passed 3,430/3,430 on .NET 10 and .NET 8, and the Word HTML adapter built for netstandard2.0 without warnings.

The [CDC agency-materials policy](https://www.cdc.gov/other/agencymaterials.html) describes reuse of federal material, with possible restrictions for photographs, marks and licensed content. Public excerpts need CDC attribution and must not imply endorsement. The article photograph and agency marks remain excluded from checked-in visual evidence pending their specific reuse terms.
