# H10 NOAA Fisheries held-out article: frozen first pass

The [H10 selection manifest](../../../../../OfficeIMO.Pdf.Benchmarks.Comparisons/Corpus/html-h10-page-selection.json) chose [2026 Pacific Coral Surveys Complete](https://www.fisheries.noaa.gov/feature-story/2026-pacific-coral-surveys-complete) on 2026-09-25, before this page was rendered or converted by OfficeIMO. Yellowstone became a development regression after its failures informed renderer work. The [NOAA Fisheries policy](https://www.fisheries.noaa.gov/national/about-us/website-policies-and-disclaimers) permits reuse of U.S.-government-created information in the United States; third-party content and agency marks need separate treatment. The article's three main photographs carry NOAA Fisheries credits. Source bytes remain only in ignored local evidence.

At clean source `cab397e0a7dcc1ab614754bccb26572a391b363d`, Chromium 151.0.7922.34 captured the page at 816 × 900 CSS pixels after `DOMContentLoaded` plus 1000 ms. The 1,438,741-byte MHTML archive has SHA-256 `f0b8d1ee436ef7c19369ad6ae71ce31ef46078444a1bb335b690e707b02a1596`, 33 MIME resources and no MIME diagnostics. Its HTML contains all four predeclared markers: the title, “By the Numbers”, “1,006 scuba dives”, and “Blue marlin larva”. The browser reference opens that exact archive offline with network disabled. Replaying it does not claim that uncaptured fonts, background images, external StoryMap content or later site changes were included.

| Output intent | Offline Chromium | OfficeIMO on the same archive | First-pass finding |
| --- | --- | --- | --- |
| Print reflow, zero margins | Four A4 pages | Five A4 pages, 90 warnings and declared loss | OfficeIMO places only the government banner and logo on page one, then starts the article on page two. Its fifth page holds just the update credit. All three credited article photos and four selected text markers survive. |
| Screen-media reflow | Five A4 pages | Six A4 pages, 284 warnings and declared loss | An off-canvas menu enters normal flow on the first managed page; Chromium shows the article on page one. The menu also adds visible links absent from the browser screen PDF. The main article, images, related news and footer remain but move one page later. |
| Fixed-canvas screen snapshot | 816 × 5294 browser PNG | Seven 816 × 900 pages, 272 warnings and declared loss | The menu overlaps the title and lead image on the first managed slice. The managed footer extends onto a seventh slice; the browser screen fits within six. |

Every page of the print and screen-media PDFs and every managed snapshot slice was rasterized and inspected. The three article images, their credits and the selected text are visible, but page counts and marker survival do not establish layout fidelity. `pdfinfo -url` finds 9 distinct destinations in Chromium print versus 10 in OfficeIMO zero-margin print, and 50 in Chromium screen-media versus 143 in OfficeIMO screen-media; the managed screen count includes links from the menu that should be hidden. The exact URL sets and visual positions need a focused comparison before making a link-preservation claim. PeachPDF independently produced a four-page print PDF; it is a comparison observation, not proof of equivalent content or styling.

The print report contains 56 distinct `HtmlRenderResourceUnavailable` sources and 13 unavailable font-face observations. The screen-media report has the same resource and font counts plus 176 `HtmlRenderPositioningModeUnsupported` observations. Many missing sources are font files and decorative stylesheet assets absent from the archive; they must be separated from resources present but mishandled. The menu and first-page placement are owned-layout or CSS-state gaps, not missing article text. This held-out page is not qualified. No renderer change was selected from its output; the frozen archive and all generated PDFs, browser PNG, report and 72-dpi page rasters are retained in `Ignore/HtmlUnknownPageQualification/h10-noaa-coral-capture-cab397e0a/` (about 16 MB).

At clean source `06206e373`, the six predeclared editable conversions used the same archive under the default 10,000-rule untrusted profile. Each operation saved and reopened its native artifact and retained all four selected text markers; each combined preparation/target report still declares loss. The preparation step embedded 11 captured image resources totaling 477,269 bytes. The three article JPEGs match the archive byte-for-byte in DOCX, XLSX and PPTX media parts, in the native OneNote file, and as hexadecimal picture data in RTF. Markdown retains all three as data-image references. The three common `HtmlRenderResourceUnavailable` diagnostics identify two related-news thumbnails and one image URL with no matching MIME part, rather than an article photograph.

| Target | Saved/reopened and independent inspection | Qualification finding |
| --- | --- | --- |
| Word | 512 KB DOCX; headings, three credited photos, selected measurements and four checked article links survive. LibreOffice renders 32 letter pages; the article starts on page 21. 28 diagnostics. | Browser-hidden navigation and an unsupported-browser notice enter the semantic document before the article. The content survives but the long document is not an acceptable article projection. |
| Excel | 392 KB XLSX; 67 populated rows in one column, four embedded image parts including the three article photos, and selected measurements in reopened cells. LibreOffice renders 19 A4 pages. 24 diagnostics. | Survey labels and values remain text in one column rather than a useful measurement grid; narrow printed cells split words and push the measurements to page two. The predeclared generic editable-grid criterion is not met. |
| PowerPoint | 389 KB PPTX; one slide, four image parts and the selected text in saved/reopened content. LibreOffice renders one slide containing only the page title and early introductory text. 22 diagnostics. | Later article sections, measurements and photos are positioned beyond the slide and are invisible in the rendered deck. The operation report does not identify this overflow. The slide-order/content criterion fails despite the markers surviving in the package. |
| OneNote | 407 KB native section; reopened headings, measurements, credits and the four checked links survive. The file contains all three article JPEG byte sequences. Six diagnostics. | The reopened HTML projection emits no image elements, so the binary preservation check does not establish how the images appear in OneNote or survive an HTML re-export. Keep visual/image qualification open. |
| RTF | 1.08 MB RTF; eight picture groups include the three article JPEGs byte-for-byte as hex, and reopened headings, measurements, credits and checked links survive. LibreOffice renders 21 letter pages; the article starts on page 12. Three diagnostics. | Hidden navigation occupies most of the first eleven pages. This is a readable semantic preservation result, not an acceptable article-first document. |
| Markdown | 665 KB saved artifact; native heading levels, the ten-item measurement list, three article data-image references and checked links survive. Four diagnostics, including generic HTML-dependent-content approximation. | The saved Markdown contains all three `Credit: NOAA Fisheries` captions, but `MarkdownDoc.Load(...).ToMarkdown()` returns only two. The missing lead-image credit has no specific round-trip diagnostic. |

The independent artifact inspector checks headings, measurement text, selected link destinations and source-image hashes; its `artifact-inspection.json` and the six complete operation reports are retained in `Ignore/HtmlUnknownPageQualification/h10-noaa-coral-editable-probe/` (about 56 MB). LibreOfficeDev 26.8.0.0.alpha0 rendered the saved Word, Excel, PowerPoint and RTF files for visual inspection; those renders are a consumer observation, not a Microsoft Office compatibility claim. Representative pages show the Word and RTF article starts, the Excel narrow-column split and PowerPoint's single visible opening slide. No editable target is qualified on this first pass merely because its file reopens or its markers survive.

Keep this archive fixed while development-case fixes address hidden-content selection, target pagination and missing round-trip diagnostics. If a fix is chosen from this held-out page's findings, move it to development and predeclare another independent replacement before making a held-out success claim. The three PDF intents, editable target-specific acceptance and supported-platform cost budgets remain open.
