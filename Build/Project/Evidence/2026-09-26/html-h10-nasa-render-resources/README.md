# NASA rendering-resource replay

The frozen 63-resource NASA gallery archive remains unchanged (SHA-256 `ca63703f97b63439d1969d7425e4e9f07c611566b6c7089e47b1b0d058739735`). At clean source `26a40069886a2b2f5552073bc8c156869e43cb55`, the HTML renderer excludes document-icon links from its render-resource manifest. General resource discovery still lists them; image preloads and visible images still load. A focused regression verifies those separate contracts.

The offline replay completed all 13 operations without failure. The explicit fit-width, local-font OfficeIMO print and Chromium print each remain four pages. Compared with the prior clean replay at `4c67a2fc8`, every OfficeIMO PDF intent has the same page count and byte-identical PDF. The fit-width print report falls from 20 to 11 warnings and from ten to one unavailable resource. The nine removed warnings were uncaptured favicon links, which do not paint in the PDF. The remaining unavailable reference is an unselected Bootstrap Icons WOFF fallback; the captured WOFF2 face supplies the visible glyphs. The operation remains `Degraded` for that fallback and other reported layout/output differences. This improves loss attribution, not NASA visual fidelity; card text and overlays, header controls, vertical spacing and footer flow still differ from the four [previously inspected page pairs](../html-h10-page-expansion/README.md#nasa-installed-faces-and-icc-png-image).

The full HTML suite passed 3,526/3,526 on .NET 10 and .NET 8; the HTML netstandard2.0 Release build had zero warnings. Exact-head H4 advanced-held-out acceptance passed 8/8 with no runner failures. The macOS static timing budget was not repeated during heavy CasaRay and iOS simulator CPU activity; the earlier WAI-head timing gate remains open. The compact replay report and H4 evidence are under `Ignore/HtmlUnknownPageQualification/h10-nasa-icons-clean-26a400698/` and `h10-h4-icons-clean-26a400698/`. The duplicated replay archive and PDFs were removed after comparing every OfficeIMO PDF hash with the retained prior replay. NASA remains a development case; the open fidelity and supported-platform gates stay in the [roadmap](../../../../../Docs/ROADMAP.md#unfamiliar-page-conversion-qualification).
