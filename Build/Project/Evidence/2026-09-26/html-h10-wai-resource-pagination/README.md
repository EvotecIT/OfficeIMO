# WAI resource and print-pagination replay

The W3C [Tables Tutorial](https://www.w3.org/WAI/tutorials/tables/) remains an H10 development page, not a qualified browser-equivalent output. Two archives are kept distinct. The original frozen MHTML has SHA-256 `c33e96c21941db20dc750f7dd01b1a9190b34b81460c7e419836dc8a3065abf9` and 10 resources. A later live capture has SHA-256 `e412cfda033d9aa02b7abd455ae0fab917c2e44ba0ad0f4bc83f233877a4397d` and 14 resources, including four browser-loaded Noto font responses (80,672 bytes). The later capture waited for all nine selected images to load. Neither archive is a substitute for the other.

At clean source `ef6cbd985ef42b00ddd3dca071946cc9ff66b5bb`, the offline replay of the later archive produced three Chromium print pages and three OfficeIMO zero-margin print pages with the captured fonts. Normal-size inspection of all three pages found the fifth table card, its float image, the WCAG resource box, and the footer on the same pages. The card's caption and image now move together to page two. Line wrapping, list indentation and small vertical spacing still differ. The same replay reports 43 OfficeIMO print diagnostics, including 22 unavailable resource URLs, seven unavailable font faces, seven unsupported SVG-content instances and four scroll-snapshot notices; the operation remains `Degraded`. The selected Noto faces are captured, but fallback font candidates, icon assets and other resources remain unresolved. The runner does not yet distinguish every unselected font fallback from a visible loss.

The original archive still produces three Chromium and three OfficeIMO zero-margin local-font print pages, but its browser reference begins page two partway through the fifth card while OfficeIMO now keeps that card together. Its uncaptured fonts and different page content make this a separate unresolved reference. The default OfficeIMO print operation also makes four pages on both archives; matching zero-margin geometry is an explicit comparison lane, not a default-print qualification. Screen-media and fixed-canvas snapshot intents remain separate open comparisons.

The pagination change records avoid-break ranges through block composition and decides at the actual page using its usable body height. A nested scroll card with a footnote previously emitted `HtmlRenderForcedFragment` and visual-fragment warnings when a nominally fitting card exceeded the footnote-reduced body height. The corrected case, a shorter left page, and a later card unaffected by an earlier footnote pass focused regressions. The full HTML suite passed 3,525 tests on both .NET 10 and .NET 8; the renderer's netstandard2.0 Release build had zero warnings. Exact-head H4 advanced held-out acceptance passed all eight cases with no runner failures.

The exact-head macOS static budget retained matching cold/warm fingerprints, 920 MB warm managed allocations and a 489 MB peak working set, but failed elapsed ceilings: 13.9 seconds cold against 8 seconds and 4.52–4.76 seconds warm against 3 seconds. CasaRay and PowerForge processes were using CPU during the run; this observation does not prove they caused the failure. Repeat the enforced budget on a quiet host before treating timing as passed or as a renderer regression. The reports and page images are retained under `Ignore/HtmlUnknownPageQualification/h10-wai-page-card-clean-ef6cbd985/`, `h10-wai-original-frozen-clean-ef6cbd985/`, `h10-h4-wai-card-clean-ef6cbd985/`, and `h10-h4-budget-wai-card-clean-ef6cbd985/`.
