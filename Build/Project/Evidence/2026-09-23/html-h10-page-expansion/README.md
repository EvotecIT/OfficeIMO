# H10 frozen-page expansion, macOS arm64

The [selection manifest](../../../../../OfficeIMO.Pdf.Benchmarks.Comparisons/Corpus/html-h10-page-selection.json) named these sources and acceptance markers before capture or OfficeIMO rendering. Capture used source commit `5ac589cd6`, an 816 x 900 CSS-pixel viewport, and live DOMContentLoaded plus 1000 ms. The archives are retained under `Ignore/HtmlUnknownPageQualification/` for exact offline replay; third-party page bytes are not committed. The manifest records each source and its rights link.

The initial clean replay used source commit `4d5009b3939f5e091f4af979f839d7aac3d1baa6` and matching owner/runner assemblies, Chromium `151.0.7922.34`, and PeachPDF `0.9.19`. Chromium replay had network access disabled. All eight PDF operations completed on each archive, with no runner failures. The H4 advanced held-out gate passed 8/8 after the PDF fix. The table below records that initial baseline; later NASA results appear below.

| Frozen source | MHTML SHA-256 | Bytes / resources | Chromium screen / print pages | OfficeIMO screen-media / snapshot / print pages | PeachPDF print pages |
| --- | --- | ---: | ---: | ---: | ---: |
| [W3C HTML guide](https://www.w3.org/MarkUp/Guide/) | `ffd7f719936ff44208cdf1e150dbdacf3e8a6d1a35b638b22aafb7c623d2d3fb` | 75,026 / 4 | 5 / 5 | 5 / 6 / 5 | 5 |
| [NASA SVS Voyager gallery](https://svs.gsfc.nasa.gov/gallery/voyager/) | `0eef6666f91254dcfb57471cbd31b0f81c3cc2e79fb6d574c60e1fdfd4b6f666` | 4,929,842 / 23 | 4 / 4 | 17 / 20 / 3 | 3 |
| [Playwright TodoMVC](https://demo.playwright.dev/todomvc/) | `f9a8008271c52496e8264aee8001c0b46977e39a6910946ca766fc738585f340` | 10,901 / 2 | 1 / 1 | 1 / 1 / 1 | 1 |

First-page PDF raster inspection and extracted text show different outcomes by intent:

- W3C print keeps both required markers and the document text. At zero margins its page count matches both references, but its top navigation, heading, image alignment and paragraph density differ from Chromium. Equal pages are not visual equivalence.
- NASA exposed the highest-impact layout gap in this baseline. Chromium print keeps the heading, overview, sidebar and two-column gallery together on page one. OfficeIMO print placed a large dark region over nearly the entire first page and displaced or clipped content; PeachPDF print also misplaced much of this page. OfficeIMO screen-media pagination retained overview text but lost the reference styling and gallery presentation, spreading content over 17 pages; snapshot pagination took 20. Neither reference's poor print output excuses OfficeIMO's defect. Page counts across intents are not directly comparable.
- TodoMVC retains both predeclared markers in OfficeIMO and Chromium PDF text. OfficeIMO's heading is too dark and large and collides visually with the input area. PeachPDF's first page has a closer pale heading but omits the input prompt from rendered and extracted output. This frozen DOM proves only the post-load static surface, not interactive execution.

The NASA archive exposed two PDF conversion failures before `4d5009b39`: a missing icon-font private-use glyph aborted print conversion, and multiline image alternative text aborted screen conversion. The fix omits an unpaintable private-use icon with a reported loss and normalizes ASCII whitespace in fallback image text. Focused regressions, a full .NET 10 HTML suite pass (3,244/3,244 on rerun), and the H4 gate cover the change. One run of the full suite had an intermittent failure in the untouched concurrent detached-projection test; that test passed alone and in the full rerun, so the intermittence remains unclassified.

The NASA page was predeclared held-out, but its first capture exposed the failures and informed the fix. It is now a development case; a new independently chosen source must replace its held-out role before a held-out acceptance claim. Resource/loss reports for each replay lane, editable Word/Excel/PowerPoint/OneNote/RTF/Markdown artifacts, visual comparisons beyond the inspected first pages, and supported-platform time/allocation/peak-memory budgets remain open in the [roadmap](../../../../../Docs/ROADMAP.md#unfamiliar-page-conversion-qualification). This evidence neither establishes general browser equivalence nor a blanket PeachPDF parity claim.

Reproduce a page from its retained `source.mhtml` with `html-mhtml-evidence --mhtml <archive> --output <new-directory> --replay-browser --require-clean-source`, as documented in the [comparison runner](../../../../../OfficeIMO.Pdf.Benchmarks.Comparisons/README.md). The clean replay directories are `h10-w3c-guide-clean-4d5009b39`, `h10-nasa-voyager-clean-4d5009b39`, and `h10-playwright-todomvc-clean-4d5009b39` under `Ignore/HtmlUnknownPageQualification/`.

## Explicit screen-resource and report follow-up

Source `666c0b77ab28e607cae10ebc9d80488c2f47e51a` adds an MHTML-aware explicit PDF request path. The previous comparison runner sent screen intents through the generic HTML adapter, which blocked archived stylesheets and images as remote resources. The runner now uses the MHTML bridge for both screen intents and records MIME diagnostics plus per-operation PDF warnings and loss status in schema-2 JSON. A heading shifted above the first snapshot page also no longer aborts PDF bookmark creation. The independent read-only review found a missing exact-head check for the MHTML/PDF bridge; the final gate includes that bridge and the relevant Email, HTML-core, AngleSharp-adapter and Core assemblies.

All three unchanged archives replayed with `--require-clean-source`, matching owner versions and zero operation failures. The full .NET 10 HTML suite passed 3,247/3,247; the MHTML/PDF bridge built for netstandard2.0 and net8.0 with no warnings; the H4 advanced held-out visual gate passed 8/8. The replay directories use the suffix `clean-666c0b77a` under `Ignore/HtmlUnknownPageQualification/`.

| NASA intent | Chromium | OfficeIMO before MHTML screen routing | OfficeIMO at `666c0b77a` | PeachPDF |
| --- | ---: | ---: | ---: | ---: |
| Screen-media paged | 4 pages | 17 pages | 3 pages | Not measured |
| Screen snapshot paged | Frozen screen reference | 20 pages | 2 pages | Not measured |
| Print | 4 pages | 3 pages | 3 pages | 3 pages |

The screen counts changed because the archived CSS and images now reach the managed renderer. First-page raster inspection at this stage confirmed that the screen snapshot had the sidebar and overview layout absent from the previous unstyled output. It still clipped the title and mispainted or omitted gallery cards. Print still put a dark field over the first page and displaced content at this stage. These were layout and paint gaps, not proof of a remaining screen-resource routing failure. The NASA archive has only 23 saved resources; the OfficeIMO report records 50 unavailable resource references per print or screen-media operation, including assets outside the saved archive. Some gallery assets are saved yet still do not appear correctly, so capture completeness and paint/layout require separate investigation. W3C and TodoMVC page counts did not change. W3C still reports one missing CID stylesheet resource; TodoMVC reports a form-field typography approximation.

The schema-2 report measures operation-level loss, but raw warning counts include favicons and offscreen references and should not be read as visible-content loss counts. Editable-format artifacts, full-page visual scoring, and supported-platform allocation and peak-memory budgets remain open.

## Flex pagination follow-up

The NASA print defect was partly caused by treating a tall flex row as an unbreakable block and by dropping the paged `body` flex layout. The renderer now derives row break opportunities from child blocks while respecting their line break constraints. It retains a simple paged flex-column body when no descendant requests a page transition and the first/left/right page geometry agrees. Bodies with authored breaks or named pages still take the existing child-block path; regressions cover direct and nested named pages, changed page geometry, a row gap adjacent to a forced break, and nested widows/orphans. The full .NET 10 HTML suite passed 3,254/3,254 and the H4 advanced held-out gate passed 8/8 on the candidate.

On the same frozen NASA archive, OfficeIMO print, screen-media pagination and screen-snapshot pagination each produced two pages; the pinned PeachPDF print lane produced three. First-page raster inspection shows the OfficeIMO title, overview and sidebar again, but the header still clips and the gallery cards and imagery remain absent or mispainted. The page-count change therefore does not establish visual equivalence with Chromium print (four pages) or general superiority to PeachPDF. This archive is a development case; a new predeclared held-out page, complete resource/loss accounting, whole-document visual checks and the editable targets are still required for H10 acceptance.

## Gallery resource and paint follow-up

The first NASA gallery thumbnails are present in the MHTML archive and were not among the unavailable-resource warnings. The renderer was dropping block content inside an inline card link, including its positioned image. It now retains that block layout, the descendant image and text links, the empty generated box that reserves Bootstrap's 16:9 image space, and percentage image height against the positioned containing block. Empty inline-block pseudo boxes are also painted. Focused regressions cover these contracts. A read-only review identified and confirmed fixes for three adjacent cases: inline-block pseudo geometry, text links under block-containing anchors, and silent image omission when PDF raster preparation returns no resource.

One captured PNG (`HelioAware_Ep1_Thumb_v2.png`) has an IDAT chunk length larger than the remaining archive part and cannot be embedded. The PDF adapter now records `HtmlPdfImagePayloadOmitted` and continues. This is a captured-byte defect, separate from the renderer defect that hid valid thumbnails. The candidate produced three OfficeIMO print pages, three screen-media pages and three screen-snapshot pages; PeachPDF print also produced three, while the frozen Chromium print reference has four. First-page raster inspection confirms a Voyager thumbnail is visible, but the card overlay text is cramped and vertical, the header is clipped, and the gallery does not match the browser's two-column composition. Equal page counts are not visual parity. The full .NET 10 HTML suite passed 3,260/3,260 and the H4 advanced held-out gate passed 8/8 on this candidate.

The final exact-head reports are retained under `Ignore/HtmlUnknownPageQualification/` with the `gallery-clean` suffix. NASA remains a development case. Complete offline browser comparison across all pages, source-specific loss accounting, replacement held-out evidence, editable target artifacts and supported-platform resource budgets remain open.

At source `d1acf2415`, positioned percentage width is resolved against the final containing block rather than the width used when the element was registered. A focused grid-area regression fails without this change and passes with it; the frozen NASA card's bottom overlay also expands from one CSS pixel to the card width. The .NET 10 HTML suite passes 3,269/3,269 and the H4 advanced held-out gate passes 8/8. The clean NASA replay has no operation failures, with four Chromium print pages and three OfficeIMO print pages. All print pages were inspected at 96 dpi: the first card label is now horizontal and readable, but OfficeIMO still clips the heading under the header and paints far fewer gallery cards than Chromium's two-column pages. Remaining gallery flow/fragmentation and resource capture issues are distinct from this positioned-width fix. The exact-head report and inspected rasters are retained in `Ignore/HtmlUnknownPageQualification/h10-nasa-positioned-width-d1acf2415/`.

## TodoMVC text paint and line-height follow-up

The frozen TodoMVC print CSS uses `font-size: 100px` on the heading and inherits the body's `line-height: 1.4em`. Offline Chromium computes the heading line box at 19.6 CSS pixels, keeps its text above the input, and paints it with `rgba(175, 47, 47, .15)`. OfficeIMO had recalculated the inherited `em` value against the heading's 100-pixel font, painted the oversized glyph at the line-box origin, and discarded foreground alpha on the PDF text path. The static renderer now inherits the parent's computed length-based line height, keeps unitless multipliers relative to each child font, and centers oversized glyph paint around a short line box. The HTML-to-PDF adapter and shared PDF drawing writer retain foreground alpha.

On the unchanged archive, the zero-margin OfficeIMO print output now separates the heading from the input and retains both text markers. It still differs from Chromium in heading glyph metrics and placement; the placeholder remains dark and upright because its vendor placeholder pseudo style is not applied. The print, screen-media and snapshot routes each produce one page, as do Chromium and the pinned PeachPDF print routes. This is a fixed layout defect with a remaining typography/pseudo-style gap, not a claim of visual parity.

Independent review exposed two adjacent regressions before the checkpoint: expanded glyph paint could make short line boxes lose text at a page break, and an outlined font could apply foreground alpha twice. Fragmentation now uses the line's layout height separately from its paint height, and the PDF adapter applies an outer opacity group only to non-outlined text. Focused tests cover a four-line, two-page oversized heading and a translucent variable-font outline. The .NET 10 HTML suite passes 3,267/3,267, the focused PDF drawing tests pass 5/5, and the H4 advanced held-out gate passes 8/8. The final exact-head report is retained under `Ignore/HtmlUnknownPageQualification/h10-playwright-todomvc-leading-clean/`.

## TodoMVC placeholder follow-up

Source `b9ed59604af30a90102695577d208f83233151ae` retains `::placeholder` and `::-webkit-input-placeholder` author rules through the owned CSS cascade and applies their text color and font style only to empty controls. A PDF widget that cannot preserve a different placeholder font falls back to static paint with an operation-level approximation warning. The focused control contracts pass, the .NET 10 HTML suite passes 3,268/3,268, and the H4 advanced held-out gate passes 8/8.

The unchanged TodoMVC archive replay completed all three OfficeIMO PDF intents, browser screen and print, and the pinned PeachPDF print lane with no operation failures. Its zero-margin print remains one page and the prompt is now pale, matching the archived CSS color. First-page inspection still finds a wider input, larger heading, and full-width notice where Chromium has a centered card. This is a qualified placeholder improvement, not page-level browser equivalence. The exact-head report and first-page rasters are retained under `Ignore/HtmlUnknownPageQualification/h10-playwright-todomvc-placeholder-b9ed59604/`.

## NASA flex cascade and all-page replay

At source `4f6acdf9e`, the stylesheet fallback no longer lets a synthesized `flex` shorthand displace an authored inline `flex: none` when a rule declares only `flex-shrink: 0 !important`. The renderer also compares shorthand and longhand cascade priority and retains their authored order. Focused tests cover both important-declaration directions and both source orders. The full .NET 10 HTML suite passed 3,274/3,274; the H4 advanced held-out acceptance gate passed 8/8. A targeted independent review found no remaining actionable issue in this flex-priority slice.

The unchanged offline NASA archive completed every replay operation at this exact head with no execution failures. Chromium print has four pages; OfficeIMO print has twelve, screen-media pagination ten, and screen-snapshot pagination twelve. PeachPDF print has three. Every printed page was inspected from page rasters. OfficeIMO now flows the gallery cards in order instead of overlapping them, but keeps one card per row, carries the gallery across pages 2-11, and leaves page 12 nearly empty. Chromium print fits two gallery cards per row on its first page. The top navigation and logo remain clipped or incomplete in OfficeIMO. This is a material flow fix, not page-level visual equivalence.

The archive contains 24 MIME parts: one HTML page, three stylesheets, fifteen JPEGs, three PNGs and two SVGs. The OfficeIMO print report records 50 unavailable resource references: 39 gallery/media URLs, nine favicons and two icon-font URLs. The archived `HelioAware_Ep1_Thumb_v2.png` is truncated and cannot be decoded even though it is present; its omission is reported separately. Valid captured thumbnails appear in the output, while many later missing images are absent from the offline Chromium reference too. The report also contains five SVG-content warnings. A direct reader probe accepts the saved USA.gov logo, but rejects the saved NASA logo because its XML declares a DTD, which the bounded SVG reader prohibits. The resource warnings therefore mix capture omissions, one malformed saved image and renderer limits; their raw count is not a visible-content loss count.

Print layout needs a separate fit-width investigation. In the offline Chromium DOM with print media emulated, the gallery list is 520 CSS pixels wide at an 816-pixel viewport, so its 310-pixel cards form one column. At a 1,024-pixel viewport the list is 744 pixels wide and forms two columns. Chromium's A4 PDF nevertheless shows two columns. Separate PDF captures at browser viewport widths 600, 816, 1,024 and 1,200 all produced four pages with the same inspected first-page text coordinates. The PDF print layout in this runner is therefore independent of the browser viewport setting. Its observed card spacing is 160 PDF points for cards separated by 320 CSS pixels in the wide DOM, consistent with roughly two-thirds of the usual CSS-pixel-to-point scale; this does not establish the browser's exact fitting algorithm. The next renderer change must keep print layout width, physical page size and scale explicit, then compare every page again. The exact-head report and all-page contact sheets are under `Ignore/HtmlUnknownPageQualification/h10-nasa-flex-priority-4f6acdf9e/`.

## NASA print fitting and missing-image fallback

Source `5b8b461e5` adds an opt-in 1,200-CSS-pixel print layout width while retaining the physical A4 PDF page size. It rejects conflicting authored page geometry, and scales visual content, links, destinations, outlines and interactive form appearances together. A separate missing-image fallback change lets the authored background show through a transparent placeholder. Focused tests cover page dimensions, a two-column row, links, fields, destinations, form appearance spacing and the background fallback. The full .NET 10 HTML suite passed 3,278/3,278 and the PDF suite passed 7,579/7,579. The HTML/PDF bridge built without warnings for .NET 8 and netstandard2.0. The exact-head H4 advanced held-out acceptance gate passed 8/8 with no failures.

The unchanged NASA MHTML archive replayed offline at that clean source head with no operation failures. OfficeIMO's explicit fit-width lane and Chromium print each produced four A4 pages; OfficeIMO's ordinary print lane still produced twelve, and PeachPDF print produced three. All four fit-width/Chromium page pairs were rasterized and inspected. The gallery now flows in two columns with broadly similar page breaks and authored dark fallback panels, but the top header clips, several card labels remain cramped, and the footer differs. The last-page third card extends beyond the gallery viewport because this group is an authored horizontal carousel; the frozen Chromium print clips it similarly. Equal page count is therefore not page-level visual equivalence. The fit-width operation reports 50 unavailable resource references, including archive-absent gallery/media and font URLs; one captured PNG remains malformed and omitted. The exact report, PDFs, page rasters and contact sheet are retained in `Ignore/HtmlUnknownPageQualification/h10-nasa-printfit-clean-5b8b461e5/`.

The macOS static budget produced deterministic 76-output fingerprints and stayed within allocation, memory and output-size ceilings. Its rebuilt exact-source run exceeded the 3,000 ms warm-iteration time ceiling in iterations 2 and 3 (3,929 and 3,411 ms) while an unrelated iOS simulator and CasaRay process were consuming substantial CPU. Timing qualification remains open; the failed run is retained in `Ignore/HtmlUnknownPageQualification/h10-budget-printfit-rebuilt-5b8b461e5/`. The first run using an older budget-runner assembly passed, but is not treated as evidence for this source head. Remaining H10 work includes the named visual differences, replacement held-out pages, editable format artifacts and supported-platform budgets.

## NASA SVG logo with a legacy DTD

At source `6105f5144`, the bounded SVG reader ignores a declared external DTD without resolving it or expanding custom entities. The unchanged NASA archive contains a saved SVG logo with a public SVG 1.1 DOCTYPE. The previous reader rejected it and the fit-width PDF report contained five `HtmlRenderSvgContentUnsupported` warnings. The exact-head offline replay now paints the NASA logo in the repeated print header and reports no SVG-content warning in any OfficeIMO PDF intent. A regression accepts a harmless external declaration and still rejects an external entity reference.

Chromium and OfficeIMO's explicit fit-width print lane still produce four A4 pages each. Every page was rasterized and compared in the retained contact sheet; the title still overlaps the header on page one, and gallery-card text, later-page flow and the footer still differ from Chromium. The page remains a development case, and the remaining resource and visual losses are not closed by this SVG fix. The replay report, PDFs, page rasters and contact sheet are under `Ignore/HtmlUnknownPageQualification/h10-nasa-svg-dtd-6105f5144/`.

The focused SVG Drawing suite passed 540/540, the HTML SVG tests passed 118/118, and the exact-head H4 advanced held-out gate passed 8/8 with no failures. The broader Drawing suite passed 2,482/2,484; the two failing tests concern conic-gradient color and a project-reference architecture check outside the SVG reader path. Their causes remain to be investigated separately.

## TodoMVC centered body in paged output

At source `4a3c73a30`, paged rendering keeps an authored `body` max-width and resolves its auto side margins instead of laying out its children against the full page. The unchanged TodoMVC archive declares `max-width: 550px; margin: 0 auto` on `body`. Its exact-head offline replay now centers the yellow notice and the white input card in both the normal and browser-user-agent print lanes. The print, screen-media and screen-snapshot intents remain one page, with no operation failures or `HtmlRenderPagePseudoGeometryPending` warning. The heading font metrics and input typography still differ from Chromium, so the corrected widths are not a full-page visual match.

The initial root-box change exposed a related page-geometry defect: a later page with a different width retained the first page's centered offset. Root blocks now carry their layout viewport and reflow at a page-width change. A two-page regression uses 300px then 500px pages and verifies a 200px body moves from x=50 to x=150 under both document and browser user-agent styles. The full .NET 10 and .NET 8 HTML suites each passed 3,306/3,306; the netstandard2.0 HTML build and exact-head H4 advanced held-out gate passed without warnings or failures. The retained report and print raster are under `Ignore/HtmlUnknownPageQualification/h10-todomvc-maxwidth-clean-4a3c73a30/`.

## SVG metadata with a legacy DTD

Source `402077f2b` brings SVG image metadata identification into line with the bounded drawing reader. A harmless external DTD is ignored with external resolution disabled, so a height-only SVG uses its intrinsic aspect ratio. A DTD-bearing SVG is read through to the end before its dimensions are accepted; unresolved custom entities still fail content validation. Ordinary SVG metadata probes keep their root-header-only behavior even after a long leading comment. The focused image-reader tests passed 111/111, the final .NET 10 HTML suite passed 3,307/3,307, the new .NET 8 SVG sizing test passed, and the netstandard2.0 HTML build completed without warnings. Independent review identified the long-comment edge case before the final test and fix.

The unchanged NASA archive replayed offline at this clean source head with no operation failures, and the H4 advanced held-out acceptance gate passed 8/8. The explicit fit-width lane and Chromium print both produced four A4 pages. All four OfficeIMO fit-width page rasters were inspected. The logo now uses the saved SVG's actual 109.998:92 ratio under its authored 40px height, but the print header and gallery still differ materially from Chromium: the title overlaps the header on page one, the repeated header title is not visible on page four, card labels remain cramped, and the later-page flow and footer diverge. This metadata correction does not close NASA's page-level visual gap. The exact-head report, PDFs and inspected rasters are retained under `Ignore/HtmlUnknownPageQualification/h10-nasa-svg-metadata-402077f2b/`.

## NASA nested flex navigation measurement

At source `e2b543f52`, an auto-width flex row measures its nested row's in-flow children, including block-link padding and generated icon text. Intrinsic collection no longer registers positioned children before their final containing width is known; the same rule applies to inline-flex and `display: contents` descendants. Zero flex-basis children contribute their max-content width, with child `min-width` and `max-width` constraints applied. Focused regressions cover those contracts. The full .NET 10 and .NET 8 HTML suites each passed 3,312/3,312; the netstandard2.0 HTML build had no warnings. An independent review found the positioned-registration and zero-basis cases, then its targeted confirmation found the min/max case before the final fix.

The unchanged NASA archive replayed offline from this clean head with no operation failures, and H4 advanced held-out acceptance passed 8/8. OfficeIMO's explicit fit-width print and Chromium print each remain four A4 pages; ordinary OfficeIMO print remains twelve and PeachPDF print three. All four fit-width page rasters were inspected. The main Voyager title now clears the fixed navbar on page one. Gallery labels and images still differ from Chromium, and the footer differs. The third card visible at the right edge of page four is also clipped by the authored carousel in Chromium print. A second exact-head replay produced a byte-identical fit-width PDF.

The first Poppler raster inspection also suggested that the repeated navbar brand disappeared on later pages. A follow-up on the same PDF found the complete brand in extracted text on every page and identical brand positions and clip groups in the HTML render tree. On direct inspection of the retained images, `pdftoppm` omits the brand only on page three; `pdftocairo` and macOS PDFKit paint it on pages three and four. This is a rasterizer-specific discrepancy, not evidence of an HTML layout defect in the brand. The inspected `fit-3.png`, `cairo-page3.png`, and `pdfkit-page-3.png` are retained beside the report in `Ignore/HtmlUnknownPageQualification/h10-nasa-nav-flex-e2b543f52/`. The same directory retains `browser-print-page4.png`, which shows Chromium also clipping the carousel's third card. This change closes only the title-overlap portion of the NASA header gap; the remaining visual work stays in the [H10 roadmap](../../../../../Docs/ROADMAP.md#unfamiliar-page-conversion-qualification).

## NASA gallery title clamping

At source `c2ffa6eab`, the owned CSS grammar retains `display: -webkit-box` and `-webkit-box-orient: vertical`. A clamped vertical box with at most one child element uses the existing line layout; a box with multiple child items keeps vertical flex stacking. The frozen NASA cards use a one-line clamp on their title links. A regression first reproduced their five-line overflow, and a second regression covers the multi-item stacking path exposed by independent review. The final .NET 10 HTML suite passed 3,328/3,328, focused .NET 8 cases passed 5/5, and the exact-head H4 advanced held-out gate passed 8/8. The macOS static budget passed with a 5,437.9 ms cold run, 1,987.2 ms maximum warm run, and 480,051,200-byte sampled peak.

The unchanged NASA archive (`0eef6666f91254dcfb57471cbd31b0f81c3cc2e79fb6d574c60e1fdfd4b6f666`) replayed offline from this clean head with zero operation failures. OfficeIMO's fit-width print and Chromium print each remain four pages; ordinary OfficeIMO print remains twelve and PeachPDF print three. All four fit-width pages were rasterized and inspected against the browser reference. Card titles now stay on one line with an ellipsis instead of wrapping over the image, and the final PDF and page rasters are byte-identical to the inspected candidate. This does not close the gallery: typography and some card content still differ, the header navigation and footer layout remain mismatched, and the archive has missing or malformed assets. The fit-width operation still reports 78 warnings, including 50 unavailable resource references. Exact reports, PDFs and inspected page rasters are retained under `Ignore/HtmlUnknownPageQualification/h10-nasa-legacy-clamp-clean-c2ffa6eab/`; the H4 and budget reports are under the matching `h10-h4-legacy-clamp-clean-c2ffa6eab/` and `h10-h4-budget-legacy-clamp-clean-c2ffa6eab/` directories.

## TodoMVC installed-face typography baseline

The unchanged TodoMVC archive replayed offline at clean source `17fc174b4`, with no operation failures and one Chromium and OfficeIMO print page each. Its zero-margin local-font page and the browser page were rasterized and inspected. OfficeIMO keeps the notice and input centered, the pale heading above the field, and the prompt visible, but the heading is much heavier and the prompt lacks the browser's light italic face. The browser PDF embeds Helvetica Neue UltraLight for the heading and LightItalic for the prompt; the managed PDF embeds Regular and Italic. The stylesheet requests `font-weight: 100` for the heading and inherits weight 300 into the input. The HTML style resolver retains numeric weights, but the positioned text and PDF path currently reduce them to bold/italic choices. On this macOS host the shared installed-font loader can find the named UltraLight, Light and LightItalic faces, so availability alone does not explain the mismatch. This identifies a shared font-selection contract to complete and verify across platforms, not a reason to special-case this page. The exact report, PDFs and inspected rasters are retained in `Ignore/HtmlUnknownPageQualification/h10-todomvc-baseline-17fc174b4/`; the heading and input typography remain open.

## TodoMVC numeric installed-font faces

At clean source `423e12c00`, the PDF path selects installed faces using the requested numeric weight and slant and checks glyph coverage against the actual text run. Distinct face programs retain distinct registration names even when their font metadata names match. The unchanged TodoMVC archive replayed without operation failures; Chromium print and OfficeIMO's zero-margin local-font print each produce one page. The managed PDF now embeds Helvetica Neue UltraLight, Light, and LightItalic programs alongside Regular. Both print pages were rasterized at 96 dpi and inspected: the heading and input prompt now use the thin and light-italic faces visible in Chromium. The OfficeIMO heading still sits lower, the notice link lacks Chromium's blue underline, and the input shadow differs. The form-field typography warning remains, so this is a typography improvement rather than a full-page fidelity closure.

The numeric-face and mixed-script coverage regressions passed on .NET 10 and .NET 8, the PDF netstandard2.0 build had no warnings, and the full .NET 10 HTML suite passed 3,329/3,329. The exact-head H4 advanced held-out gate passed 8/8 with no operation failures. The macOS static budget passed with a 5,478.4 ms cold run, 2,003.9 ms maximum warm iteration, a 453,967,872-byte sampled process-tree peak, and identical fingerprints across all 76 outputs per iteration. The clean-source replay, PDFs and inspected page rasters are under `Ignore/HtmlUnknownPageQualification/h10-todomvc-numeric-faces-clean-423e12c00/`; H4 acceptance and budget reports are under the matching `h10-h4-numeric-faces-clean-423e12c00/` and `h10-h4-budget-numeric-faces-clean-423e12c00/` directories. Installed-face availability and visual output on other platforms remain unverified.

## NASA print media width

At clean source `5e334f266`, print fitting retains the captured 816px viewport for CSS width media queries and responsive resource selection while laying out at 1200px and scaling onto A4. A Chromium PDF probe on the frozen archive selected its medium-width breakpoint at the 816px viewport; the prior OfficeIMO fit lane selected the large-width breakpoint. The change also gives nested `srcdoc` frames and SVG `foreignObject` content their own media-query widths. Focused regressions cover inline and element media rules, a linked stylesheet, a picture source, and a narrow frame inside fitted print. Independent review found the inherited-frame-width defect and confirmed its correction. The final .NET 10 and .NET 8 HTML suites each passed 3,335/3,335, and exact-head H4 advanced held-out acceptance passed 8/8. The macOS static budget passed: 5,340.3 ms cold, 1,958.3 ms maximum warm, 487,784,448-byte sampled peak, with deterministic cold and warm fingerprints.

The unchanged NASA archive (`0eef6666f91254dcfb57471cbd31b0f81c3cc2e79fb6d574c60e1fdfd4b6f666`) replayed offline with no operation failures. The fit-width OfficeIMO print and Chromium print each produced four A4 pages; ordinary OfficeIMO print produced twelve, and PeachPDF print produced three. All four fit-width/Chromium page pairs were rasterized and inspected. The sidebar and two-column gallery now start close to Chromium's positions on page one, but card descriptions and overlays, header typography, and footer layout still differ. The fit-width report remains degraded with 78 warnings, including 50 unavailable resources and one malformed captured image. This closes the media-breakpoint mismatch, not NASA's page-level fidelity gap. The exact-head report, PDFs and page rasters are retained under `Ignore/HtmlUnknownPageQualification/h10-nasa-media-width-clean-5e334f266/`; H4 and budget reports are under the matching `h10-h4-media-width-clean-5e334f266/` and `h10-h4-budget-media-width-clean-5e334f266/` directories.

## NASA missing-image alternative layout

At clean source `81228228a`, missing-image alternative text wraps inside its authored image box and is clipped at the box boundary. The wrapped prefix is bounded before layout; a clipped remainder retains the long alternative in the PDF text stream. Focused render and PDF regressions passed, as did the full .NET 10 and .NET 8 HTML suites (3,338/3,338 each). An independent read-only review found no actionable defect in the changed layout or text paths. The exact-head H4 advanced held-out gate passed 8/8. The macOS static budget passed with a 5,325.7 ms cold run, 1,961.4 ms maximum warm run, and 648,855,552-byte sampled peak.

The unchanged NASA archive replayed offline with no operation failures. OfficeIMO fit-width print and Chromium print each remained at four pages. All four page pairs were rasterized and inspected. On pages two and three, the absent gallery images now show multiline alternatives inside their cards, much closer to Chromium's fallback layout than the previous single clipped line. Header typography, card overlays, some text metrics, and footer layout still differ. The fit-width operation reports 77 warnings, including 50 unavailable resources; wrapping does not recover missing assets. Exact reports, PDFs, and inspected page rasters are retained under `Ignore/HtmlUnknownPageQualification/h10-nasa-alt-wrap-clean-81228228a/`; H4 and budget reports are under the matching `h10-h4-alt-wrap-clean-81228228a/` and `h10-h4-budget-alt-wrap-clean-81228228a/` directories. The full long alternative's exact extracted text, image/page-break edge cases, and other platform budgets remain outside this checkpoint's focused proof.

## NASA responsive brand at zero font size

At clean source `debfeb0ac`, the renderer honors an authored `font-size: 0` on the NASA navbar brand while retaining its visible `::after` text. It now paints “SVS” instead of the longer hidden label. Intrinsic flex measurement and generated leader/page-counter text also exclude zero-size text, while descendants that set a visible font size still contribute their width. Regressions for these paths passed in the full .NET 10 and .NET 8 HTML suites (3,341/3,341 each). An independent read-only review found the adjacent flex and generated-content paths; its targeted confirmation found the anonymous flex text path, which was fixed before this clean run.

The unchanged NASA archive replayed offline with no operation failures. OfficeIMO fit-width print and Chromium print each have four pages; ordinary OfficeIMO print still has twelve. All four clean-head OfficeIMO and Chromium page rasters are byte-identical to the previously inspected four-page candidate pair, so this checkpoint does not change the rest of the page composition. The fit-width conversion remains degraded with 77 warnings, including 50 unavailable resources. The header navigation and icons, font faces and metrics, card overlays, and footer still differ from Chromium. The exact report, PDFs and page rasters are retained under `Ignore/HtmlUnknownPageQualification/h10-nasa-zero-font-clean-debfeb0ac/`; H4 acceptance passed 8/8 under the matching `h10-h4-zero-font-clean-debfeb0ac/`. The macOS static budget passed with a 6,517.3 ms cold run, 1,995.6 ms maximum warm run, 449,675,264-byte sampled peak, and deterministic fingerprints under `h10-h4-budget-zero-font-clean-debfeb0ac/`. Other supported-platform budgets and the remaining page-level gaps stay open.

## NASA navigation control sizing

At clean source `f7b4ec3ed`, nested flex rows include the intrinsic widths of their form controls. Empty icon-only buttons no longer acquire invented visible “Button” text, while an inline SVG still contributes to its button's width. Focused regressions cover the nested search row, empty button and SVG button. Independent review found the SVG sizing regression in the candidate; it was corrected before the clean run. The final .NET 10 and .NET 8 HTML suites each passed 3,345/3,345. An initial .NET 10 run had one detached-node identity concurrency failure outside the touched layout code; that case passed alone and the full suite passed on rerun. The H4 advanced held-out gate passed 8/8, and the macOS static budget passed with a 5,482.5 ms cold run, 2,044.8 ms maximum warm run, 448,593,920-byte sampled peak and deterministic fingerprints.

The frozen NASA archive replayed offline from this source with zero operation failures. OfficeIMO fit-width print and Chromium print each produce four pages; ordinary OfficeIMO print still produces twelve. The first-page raster shows “Galleries” and “Help” closer to their browser positions, and no invented button labels. The search field and icons remain visually absent, even though the PDF text stream contains search text. Page one was inspected against Chromium; pages two through four are byte-identical to the previously inspected candidate rasters. The fit-width operation still reports 77 warnings, including 50 unavailable resources. Font metrics, card overlays and footer layout remain mismatched. Reports, PDFs and page rasters are retained under `Ignore/HtmlUnknownPageQualification/h10-nasa-nav-controls-clean-f7b4ec3ed/`; H4 and budget reports are under the matching `h10-h4-nav-controls-clean-f7b4ec3ed/` and `h10-h4-budget-nav-controls-clean-f7b4ec3ed/` directories. This checkpoint improves navigation sizing but does not complete the NASA fidelity gate or the broader H10 qualification.

## NASA search field flex width

At clean source `6ef61ce58`, a row flex child paints at its resolved flex width after growth or shrinkage. The NASA search input had a `width: 1%` declaration: flex allocated it a wider slot, but paint used the narrow authored width and hid its placeholder. Regressions cover that input, ordinary grow/shrink sizing, and padded tables. Independent review found that tables interpret explicit width as a border-box width and that a content-box `max-width` could clamp it twice; both cases were reproduced and corrected before the clean run. The full .NET 10 and .NET 8 HTML suites each passed 3,350/3,350. The H4 advanced held-out gate passed 8/8. The macOS static budget passed with a 4,667.1 ms cold run, 1,931.6 ms maximum warm run, 517,324,800-byte sampled peak, and deterministic fingerprints.

The unchanged NASA archive replayed offline with no operation failures. All four OfficeIMO fit-width and Chromium print page pairs were rasterized and inspected. The navbar now shows “Search...” on each OfficeIMO page. Its border and icons still differ from Chromium, and the gallery cards, text metrics, missing resources, and footer remain visibly mismatched. Fit-width print and Chromium print each remain four pages; ordinary OfficeIMO print remains twelve. The fit-width operation remains degraded with 77 warnings, including 50 unavailable resources. Exact reports, PDFs, and inspected page rasters are retained under `Ignore/HtmlUnknownPageQualification/h10-nasa-flex-used-width-clean-6ef61ce58/`; H4 and budget reports are under the matching `h10-h4-flex-used-width-clean-6ef61ce58/` and `h10-h4-budget-flex-used-width-clean-6ef61ce58/` directories. This is a search-field layout correction, not completion of the NASA fidelity gate or broader H10 qualification.

## NASA search border cascade

At clean source `e6f37a731`, the provider fallback no longer treats a synthesized `border` shorthand from a color-only rule as an authored border. On the frozen NASA page, that synthetic shorthand displaced the variable-backed width and style in the search input's important border declaration. A focused regression covers the shorthand, important color longhand, and right-edge reset; the full .NET 10 and .NET 8 HTML suites each passed 3,351/3,351. Independent read-only review found no actionable defect. H4 advanced held-out acceptance passed 8/8, and the macOS static budget passed with a 7,342.7 ms cold run, 2,417.6 ms maximum warm iteration, a 435,257,344-byte sampled peak, and deterministic fingerprints.

The unchanged NASA archive replayed offline at this clean head with no operation failures. OfficeIMO fit-width print and Chromium print each have four pages; ordinary OfficeIMO print has twelve. All four fit-width/Chromium page pairs were rasterized and inspected. The search input now has its light border with the right edge suppressed, closer to Chromium on page one. OfficeIMO still repeats that field on later print pages where Chromium does not show it, and the gallery overlays, typography, missing icons and resources, and footer remain visibly different. Fit-width conversion is still degraded with 77 warnings, including 50 unavailable resources. Exact reports, PDFs, and inspected page rasters are retained under `Ignore/HtmlUnknownPageQualification/h10-nasa-border-clean-e6f37a731/`; H4 and budget reports are under the matching `h10-h4-border-clean-e6f37a731/` and `h10-h4-budget-border-clean-e6f37a731/` directories. This closes the search border defect only; NASA page fidelity and the broader H10 qualification remain open.

## TodoMVC link paint and authored decoration

At clean source `9121fe7dd`, the owned style resolver applies the browser's blue underlined default to links with `href`, while an authored color or `text-decoration: none` overrides it. The owned CSS cascade now expands the supported `text-decoration` shorthand in declaration order. Nested inline text keeps its ancestor's underline and decoration color even when the child declares `text-decoration: none`; an atomic inline-block stops that propagation. `revert` and a no-prior-layer `revert-layer` return to the link default. A focused regression covers these contracts, including an authored dotted red decoration. The full HTML suite passed 3,352/3,352 on both .NET 10 and .NET 8, the MHTML/PDF bridge built for netstandard2.0 without warnings, and independent read-only review confirmed the two nested/revert fixes.

The unchanged TodoMVC archive replayed offline at this source with no operation failures. Chromium and every OfficeIMO PDF intent still produce one page. The inspected local-font print page now paints the yellow notice's link blue and underlined, as Chromium does, while the footer links retain their authored plain style. The heading remains lower than Chromium's and the input shadow differs; the form-field typography approximation remains in the operation report. The exact report, PDFs and first-page rasters are under `Ignore/HtmlUnknownPageQualification/h10-todomvc-link-remediation-9121fe7dd/`. H4 advanced held-out acceptance passed 8/8; the macOS static budget passed with a 5,275.4 ms cold run, 1,892.0 ms maximum warm run, 522,731,520-byte sampled peak, and deterministic fingerprints. Those reports are under the matching `h10-h4-link-remediation-9121fe7dd/` and `h10-h4-budget-link-remediation-9121fe7dd/` directories. Other supported-platform budgets and TodoMVC's remaining typography and shadow differences stay open.

## TodoMVC heading vertical metrics

At clean source `fc4398335`, heading paint uses the selected font face's ascent and descent instead of treating line-box height as the glyph's top. The same vertical overflow reaches PDF clipping and text-shadow copies. Focused high-ascent and shadow regressions passed on .NET 10 and .NET 8; the full HTML suites passed 3,354/3,354 on both targets before the final text-shadow copy fix, and its focused tests passed afterward. Independent review exposed the face-selection and top-clipping cases, which were fixed before this checkpoint.

The unchanged TodoMVC archive replayed offline without operation failures. The local-font zero-margin PDF places the heading's painted top at about 29.4 pt versus Chromium's 29.25 pt; the prior OfficeIMO top was about 40 pt. Its prompt also remains near the browser position. This closes the heading placement defect on the frozen page, not page-wide visual fidelity. The report and PDFs are under `Ignore/HtmlUnknownPageQualification/h10-todomvc-vertical-fc4398335/`.

## TodoMVC inset shadow fill rule

At clean source `f86a0eb7d`, the PDF writer uses an `OfficeShape.Path` fill rule for ordinary and transformed fills, fill-and-stroke paths, shadow layers, and gradient clipping. Before the fix, the TodoMVC input's even-odd inset-shadow rings were emitted with a nonzero fill, tinting the entire field grey. The PDF vector suite passed 7,582/7,582 and the HTML suite passed 3,354/3,354 on .NET 10; focused vector and HTML shadow tests also passed on .NET 8. An independent read-only review found no runtime defect and its test-assertion finding was corrected. The netstandard2.0 PDF build completed without warnings.

The same frozen archive (`f9a8008271c52496e8264aee8001c0b46977e39a6910946ca766fc738585f340`) replayed offline with no operation failures and one page for each PDF intent. On the inspected local-font print page, the input center is white again, as in Chromium. The outer box shadow still has a darker hard band beneath the field, and the PDF report retains the form-field typography approximation. The exact-head report, PDFs, and compared rasters are under `Ignore/HtmlUnknownPageQualification/h10-todomvc-fill-rule-f86a0eb7d/`. H4 advanced held-out acceptance passed 8/8; the macOS static budget passed with a 5,263.5 ms cold run, 1,921.2 ms maximum warm iteration, 519,897,088-byte sampled peak, and deterministic fingerprints under the matching `h10-h4-fill-rule-f86a0eb7d/` and `h10-h4-budget-fill-rule-f86a0eb7d/` directories. WAI, MDN, NASA, remaining TodoMVC shadow work, replacement held-out sources, editable outputs, and other supported-platform budgets remain open in the roadmap.

## TodoMVC outer shadow falloff

At clean source `b64e94772`, the shared Drawing shadow planner spreads the solid core of an expandable blurred shape across inset layers. The PDF, SVG, and raster consumers now paint those smaller shapes at the same shadow offset. This softens the hard horizontal band below the frozen TodoMVC input. At x=350 on the 708×1000 local-font print raster, the mean absolute difference from the Chromium reference over y=216–269 fell from 5.00 to 2.56 gray levels; the prior flat 217-level run at y=220–235 now rises from 220 to 228. The final page raster was inspected. The input and heading still differ in typography, and this centerline measurement does not establish page-wide visual equivalence.

The unchanged archive replayed all ten browser, OfficeIMO, and PeachPDF operations without runner failures, with one page per PDF intent. The report still records static form-field typography as an approximation. An independent review caught an opacity discontinuity at exactly 1; the outer inset layers now vary continuously and the deepest layer supplies the remaining opacity. Focused Drawing shadow tests passed 10/10, PDF shadow tests 10/10 on .NET 10 and .NET 8, and HTML shadow tests 17/17 on both targets. The full .NET 10 PDF and HTML suites passed 7,582/7,582 and 3,354/3,354. The broader Drawing suite passed 2,484/2,486; its conic-gradient color and project-reference architecture failures do not exercise the changed shadow path and remain separate investigation items. H4 advanced held-out acceptance passed 8/8. The macOS static budget passed with a 4,906.7 ms cold run, 1,955.7 ms maximum warm iteration, 519,143,424-byte sampled peak, and deterministic fingerprints.

The exact-head replay, compared raster, H4 report, and budget report are under `Ignore/HtmlUnknownPageQualification/h10-todomvc-shadow-clean-b64e94772/`, `h10-h4-shadow-clean-b64e94772/`, and `h10-h4-budget-shadow-clean-b64e94772/` beneath the same parent directory. WAI, MDN, NASA, remaining TodoMVC visual differences, replacement held-out sources, editable outputs, and other supported-platform budgets remain open.
